# P1 — One Live Autonomous Agent (plan, plan-first, awaiting founder sign-off)

> Phase P1 of `docs/agent-metaverse-model.md §9`. Goal: prove the FULL autonomy loop — perceive → **decide (inference)** → act → settle → render — on **ONE** ClawVille-hosted agent, end-to-end, live. Builds ON P0's lifecycle floor. NOT the fleet (P4), NOT identity migration (P2), NOT the streamflow cursor (P3).
>
> Worktree: `cv-agent-p0`, branch `feat/agent-metaverse-p0` (P1 stacks on P0; ship P0+P1 together). Model: `gpt-4o-mini`, swappable backend (fleet → open/self-hosted later). Seam-map source: workflow `wf_ef15b3f3-8cd` (4 read-only readers).

## ★ Headline correction (from the seam-map — changes the whole shape of P1)

**The autonomy loop ALREADY EXISTS and works.** It is NOT a from-scratch build:
- `SimulationRuntime.planAvatarNextAction(userId)` (`packages/agent-runtime/src/simulation/simulation-runtime.ts:215`) = the **decide** phase: composes world-state → `useModel(TEXT_SMALL)` → parses a JSON action choice.
- `SimulationRuntime.dispatchAction()` (`:338`) = the **act** phase.
- `AvatarSimulationBridge.tick()` = the live perceive→decide→act cycle, driven every 200ms from `NpcSimulation` (`npc-simulation.ts:396`, `avatarAutonomyManager.tick()` `:1637`).
- Budget guardrails already exist: `budgetMaxNt` (100 CT/session), `budgetMaxPurchases`, `BehaviorCooldown` (~20s between LLM calls) in `avatar-state-store.ts:36-48`; enforced in `planAvatarNextAction` (`simulation-runtime.ts:252`).

**Why it's "dead" today:** the ONLY missing link is the *activation entry point*. `POST /api/avatars/me/heartbeat` (`avatars.ts:1012`) is what registers an avatar into the bridge — and it has **ZERO callers in `apps/web`**. The engine is built; nothing ever turns it on.

**So P1 = ACTIVATE the existing loop for one hosted "house" agent + adapt cadence/keying + 3 genuinely-new pieces** (proximity gate, `awardInWorldAction`, the render fix). Far smaller than "build the autonomy engine."

## ★★ Plan correction (2026-07-01, POST-BUILD — supersedes the Headline's framing for the house agent)

The seam-map above described the AvatarSimulationBridge / `planAvatarNextAction` path. Verified against live code during the build, that is the **human-idle-avatar** engine (Path B: `userId`/stateStore-keyed, emits `AVATAR_*` actions, renders via `snapshot.autonomousAvatars`). **The house agent does NOT use it.** Two distinct engines — do not conflate:

- **House agent (Path A, what P1 actually builds):** an `ocb-<base64url(agentId)>` NPC in `npcSimulation.npcs` (via `registerOpenClaw` avatar-mode) that **RENDERS via `snapshot.npcs`** — the existing Hatcher-proven avatar-body render, NOT `autonomousAvatars`. Its cognition is the **`[ACTION:]` / ElizaOS path**: the new `agent-autonomy-driver.ts` calls `npcSimulation.buildPerception(bodyId)` → `ElizaRuntime.decide()` (gpt-4o-mini) → emits `[ACTION:]` tags → `dispatchHatcherActions()`. It does **NOT** call `planAvatarNextAction`/`dispatchAction` (those are Path B and emit `AVATAR_*` actions).
- **Slice 2 is ORTHOGONAL:** it fixes the Path-B (human-idle) render (bug B3 — `autonomousAvatars` had zero client consumers). It is a real, plan-listed fix but is **NOT** the house-agent render path. The "watch the house agent walk + talk" proof runs entirely through `snapshot.npcs`. (Lower risk — it rides the Hatcher-proven npcs render.)

## The one agent
A **ClawVille-hosted ElizaOS "house" agent** — internal-only `is_house` flag, avatar-mode body (`ocb-<agentId>`, `avatarBodyId()` `npc-simulation.ts:582`), its ElizaRuntime warmed via `createElizaRuntime` (`eliza-runtime.ts:846`), inference on `gpt-4o-mini`. First member of the eventual fleet. (Hatcher is a partner integration, NOT this path.)

## Slices (with real edit points)

> **BUILD STATUS (2026-07-01) — Slices 1 + 3 BUILT (API, non-money loop).** Branch `feat/agent-metaverse-p0`. Slice 2 (client render) done by another teammate; Slice 4 (settle/leaderboard) DEFERRED — the agent earns NOTHING yet. See "What shipped" at the bottom.

### Slice 1 — lifecycle-consistency (mostly FREE via P0)  ✅ BUILT
Register the house agent's body + warm its runtime at **boot** (a seeder like `ensureSystemAgents`, or `agent-orchestrator.startAgent`), NOT via the dead heartbeat. P0 already guarantees it survives restart (lazy-restore + restore-aware session-status). `reuse` `registerOpenClaw` (`npc-simulation.ts:770`) + `agent-orchestrator` (skip the 30-min inactivity stop for `is_house`, like system agents).

### Slice 2 — render the body (bug B3 fix, ~1 change)
`apps/web/src/stores/npc.ts` `updateFromSnapshot` (`:383`) only processes `snapshot.npcs`, **never** `snapshot.autonomousAvatars` — so autonomous bodies arrive over the wire and are silently dropped. **Fix:** process `autonomousAvatars[]` there (map to `NpcSpriteState` w/ entity-interp `ts/tsDelta`), reusing the existing `VRMNpcMesh`/`GLBNpcMesh` render in `arena-npcs.tsx`. No new renderer. `extend`.

### Slice 3 — autonomous action + THE PROXIMITY GATE  ✅ BUILT
- **NEW `agent-autonomy-driver.ts`** (`build-new`): its own `setInterval` at **~30s** (not the 200ms NPC tick). Iterate active house agents (needs a public iterator over `session-agent-map`/orchestrator), `buildPerception(bodyId)` (`agent-gateway.ts:1931`, `reuse`) → decide via the SimulationRuntime pattern (agent-keyed variant of `planAvatarNextAction`) → dispatch. Fire-and-forget with in-flight tracking + LLM timeout so one slow brain can't block the tick.
- **First behavior:** perceive → decide "which teacher can help me" → `enter_building` (= *walk to* the building, `:1317`) → `talk_to_npc` (the real conversation).
- **NEW PROXIMITY GATE** (`build-new`) in `executeHatcherAction` `talk_to_npc` (`npc-simulation.ts:~1394`, before `injectAgentChat` `:1416`): resolve target pos (npc `this.npcs.get(target)` or `NPC_BUILDING_CENTERS[target]`) → `dist = hypot(dx,dy)` → if `dist > BUILDING_INTERACTION_RADIUS` drop + warn. **`BUILDING_INTERACTION_RADIUS = 1000 wu`** — ONE shared constant, replacing the ad-hoc `VISIT_RADIUS = 2000` on BOTH the authed visit-building path (`agent-gateway.ts:2244`) and this gate (harmonized, single source of truth). Units: **wu = game-space px** (1 tile = 32; world = 22528 wu). 1000 wu ≈ 2 building-widths (building zone = 448 wu) / ~½ the inter-building gap (~2000 wu) — clearly "at this teacher," slack for the 80 wu pathfind stand-off, no overlap with the neighbor. This is the anti-abuse backbone: **no walk/proximity → no interaction → no reward.** ⚠️ AS BUILT (2026-07-01): the gate is **SCOPED to non-hatcher-proxy bodies** (house/fleet), NOT "every agent" — Hatcher (`hatcher-proxy`) is intentionally EXEMPT this diff (see "Deferred / tracked debt" below). The single server-side chokepoint remains; only the scope predicate defers Hatcher.

### Slice 4 — settle (one economic action)
**NEW `awardInWorldAction(avatarId, actionType, amount, metadata)`** in `claw-token-ledger.ts` (`build-new`): (1) `creditClawTokens` (atomic, txn-locked), (2) post-commit `logEvent('world_teacher_chat', …)` (leaderboard, fire-and-forget), (3) `memoryService.createMemory` (the agent *learned* something). Add `world_teacher_chat` to `AGENT_SCORE_WEIGHTS` (`leaderboard.ts:370`) + a daily cap (fold into the fp/ip squash so it can't be farmed). **Gated on the proximity-passed interaction** — reward only fires for a genuinely-present, conversed turn. (Note: today `injectAgentChat` on the autonomous path fires NO reward — this wires the first real one.)

## Model backend (swappable — per the decision: mini now, open/self-host for the fleet)
- Default `gpt-4o-mini` via `openai-text-provider.ts` (base URL hardcoded `:37`).
- **Fleet swap is already largely supported:** `openclaw-provider.ts` (priority 100, `:102`) routes a per-agent config to ANY OpenAI-compatible gateway (`protocol='openai-compat'`) — point it at a self-hosted endpoint, per-agent, no global change. For a fleet-wide flip, add an `OPENAI_BASE_URL` env knob to `openai-text-provider` (`extend`, 1 line). So the open-model migration is config, not a rebuild — confirmed.

## Guardrails (mostly exist)
Budget (`budgetMaxNt`, exists) · decision cooldown (`BehaviorCooldown`, exists) · `[ACTION:]` DoS cap (`MAX_HATCHER_ACTIONS_PER_REPLY=4`, exists) · **NEW proximity gate** (slice 3). Cost is bounded by the 30s tick + the phase machine's natural walk + 60s talk-cooldown pacing (and the planned open-model migration) — **NOT** by human presence. (An earlier human-gated idle-throttle was REMOVED 2026-07-02 — see below; it contradicted the always-on premise.)

## Verify (the proof — visible + on-ledger)
On staging, one house agent: boots → registers a body → **renders (you SEE it walk)** → every 30s decides → walks to a teacher (proximity-gated) → has a **real conversation** → the turn settles CT + leaderboard + memory → the first-ever `world_teacher_chat` / `building.visited` rows appear (currently **0** in all history). Browser-verify the walk + conversation; assert the ledger/events/leaderboard/memory rows.

## What shipped — Slices 1 + 3 (API, 2026-07-01)

Non-money perceive→decide→act loop for ONE ClawVille-hosted house agent. No CT, no leaderboard (slice 4 deferred).

- **is_house column** — `openclaw_bots.is_house BOOLEAN NOT NULL DEFAULT false` (`packages/database/src/schema/claws.ts`) + idempotent DDL `packages/database/migrations-manual/2026-07-01_add_openclaw_is_house.sql` (NOT db:push'd — orchestrator applies). Internal-only: NEVER serialized to any snapshot/roster/wire.
- **House-agent seeder** — `apps/api/src/services/house-agent-seeder.ts` (`ensureHouseAgent`), wired at boot in `index.ts` after the system-NPC seeder. Mirrors `ensureSystemAgents`: upserts the `openclaw_bots` row (is_house=true, `session_expires_at=NULL` so the 24h sweeper skips it), the `platform_agents` (openclaw-bot, NO gateway → gpt-4o-mini via openai-text-provider, provider-SWAPPABLE for the fleet) runtime warmed via `agentOrchestrator.startAgent(..., {isHouse:true})`, and the in-world avatar body via `npcSimulation.registerOpenClaw` with **protocol `nanoclaw` (NOT hatcher-proxy)**. Body renders via `snapshot.npcs` (the Hatcher-proven path), NOT autonomousAvatars.
- **Orchestrator exemption** — `RunningAgent.isHouse` + `startAgent(id, userId, {isHouse})`; `stopInactiveAgents` skips `type==='system-agent' || isHouse` (the driver drives via `useModel`, which does not bump `lastActivity`). Body-idle sweeper (`agent-body-idle-sweeper.ts`) also EXEMPTS is_house rows.
- **Autonomy driver** — NEW `apps/api/src/services/agent-autonomy-driver.ts` on its OWN 30s interval (NOT the 200ms tick). Per house agent: `npcSimulation.buildPerception(bodyId)` → `ElizaRuntime.decide` (gpt-4o-mini) → `npcSimulation.dispatchHatcherActions`. Phase machine deciding→walking→arrived→talking. In-flight guard + hard LLM timeout + bounded maps. Human-presence INDEPENDENT cadence (the idle-throttle was removed 2026-07-02 — see below). Wired start/stop into `index.ts` boot/shutdown.
- **Shared perception** — the former module-local `agent-gateway.ts buildPerception` MOVED to `npcSimulation.buildPerception(npcId)` (single source; gateway `/perception` + SSE `/events` + the driver all call it).
- **Proximity gate** — in `executeHatcherAction` `talk_to_npc` (before `injectAgentChat`): non-hatcher-proxy bodies must be within `BUILDING_INTERACTION_RADIUS` of the target or the talk is DROPPED (fail-closed). Hatcher (`hatcher-proxy`) stays exempt (§3a fast-follow). `BUILDING_INTERACTION_RADIUS = 1000` is ONE shared constant (`packages/shared/src/constants/npc-definitions.ts`) that ALSO replaced the ad-hoc `VISIT_RADIUS=2000` on the authed `visit-building` path.
- **decide()** — new `ElizaRuntime.decide(prompt)` (`useModel(TEXT_SMALL)`, provider-swappable) in `packages/agent-runtime/src/eliza-runtime.ts`.
- **Tests** — `apps/api/src/services/__tests__/agent-autonomy-p1.test.ts`: gate DROP-far / PASS-near / hatcher-exempt + driver picks-teacher-emits-enter_building. tsc 0; suite green (0 new failures vs baseline).

### Reconciler fixes (2026-07-01 — post-review punch list: B1 blocking + N1–N4)
- **B1 (blocking) — house body no longer wander-hijacked.** The seeder built the avatar with `autonomyMode: 'server-managed'`, but `planNpcBehaviors` only skips `isOpenClaw && autonomyMode==='self-managed'`, so the 200ms sim wandered the house body out from under the 30s driver (then the proximity gate correctly dropped the far talk → the walk→talk loop never completed). FIX: `autonomyMode: 'self-managed'` (also satisfies the canonical "NanoClaw is always self-managed" rule in `routes/agent-gateway.ts`). Path movement in `moveNpcs` is mode-independent (it only gates on `isDead || inConversation || isHumanControlledOpenClawNpc`), so the driver's A*-set walk paths still execute; only the autonomous PLANNER is mode-gated. Config extracted to an exported pure `buildHouseAvatarConfig(sessionId, species)` (DB-free, unit-testable).
- **N1 — dead idle-throttle now fires.** `agent-autonomy-driver.ts tick()` computed `throttledIdle` then discarded it (`void throttledIdle`), calling `driveOnce` with 2 args. FIX: pass it as the 3rd arg so the LLM cost-control cadence (back off when `getActiveHumanCount()===0`) actually suppresses the LLM-bearing phases. **(SUPERSEDED 2026-07-02 — the entire human-gated idle-throttle was REMOVED; see "Human-presence-independent cadence" below.)**
- **N2 — house agent id no longer publicly identifying.** `HOUSE_AGENT_ID` was `clawville-house-01`, which leaks via `GET /api/openclaw/active` (raw agentId) and the body id `ocb-<base64url(agentId)>` (decodes back to it). FIX: a FIXED OPAQUE uuid (`6f1d9a2c-…`) — no "house"/"clawville" substring, no reserved prefix; stays a constant for boot idempotency + the deterministic bodyId. (Name "Coralia" is unchanged — agents have names.)
- **N3 — stale destination race fixed.** After dispatching `enter_building`, the driver read `body.destinationBuildingId` to learn its choice; if the executor DROPPED the action (e.g. no path), that field held a STALE prior value → the driver "walked" to a building it did not choose. FIX: the driver CLEARS `destinationBuildingId` via new `npcSimulation.clearDestinationBuilding(bodyId)` BEFORE dispatch, so post-dispatch it is non-null ONLY if THIS turn's `enter_building` succeeded (a valid one re-stamps it via `setNpcPath`; a re-pick of the same building still works).
- **N4 — house body no longer pulled into ambient NPC↔NPC conversations.** Ambient partner selection (`getIdleAliveNpcs` / `findNearestIdleNpc` / `planApproachNpc`) did not exclude self-managed OpenClaw bodies, so an ambient NPC could converse-pair with the house body → froze it (`inConversation`) + degenerate empty nanoclaw bubbles. FIX: new `isSelfManagedOpenClawNpc(npc)` predicate excludes `isOpenClaw && autonomyMode==='self-managed'` from those three selectors — scoped tightly; Hatcher (`server-managed`) is UNAFFECTED. (The free-roamer variant `planApproachNearbyNpc` was left as-is: it is a walk-toward planner, not a freeze vector, and the conversation freeze is already fully closed by the two selector fixes.)
- **Regression guards** — `agent-autonomy-p1.test.ts` now 7/7 (added: house-config-is-self-managed [B1]; self-managed-body-excluded-from-partner-selection-but-server-managed-is-not [N4]). tsc 0 on all 4 packages; `bun test apps/api` baseline 55-fail UNCHANGED (1006 pass / 55 fail, +2 from the new guards).

### Human-presence-independent cadence (2026-07-02 — founder design correction)
The idle-throttle (N1 above: `IDLE_DECIDE_EVERY=4` → LLM-decide only every 4th tick = 120s when `getActiveHumanCount()===0`) was **REMOVED entirely**. It made a hosted agent 4× less active when alone, directly contradicting the agent-metaverse premise (the economy must flow with **zero external users** — agents ARE the world's activity, present or not; see `docs/agent-metaverse-model.md` + CLAUDE.md "Autonomous is a FULL-SCOPE economic participant … PERSIST when the user leaves"). `agent-autonomy-driver.ts` now runs the SAME decision cadence regardless of human presence: dropped the `IDLE_DECIDE_EVERY` constant, the `getActiveHumanCount()`/`throttledIdle` computation in `tick()`, the `throttledIdle` param + guard in `driveOnce`, and the `humansPresent`/`throttledIdle` debug fields. Real per-agent decide rate is naturally paced by the 30s tick + the walk time + the 60s talk-cooldown (a full decide→walk→arrive→talk→cooldown loop is ~2–3 min regardless), so the always-on cost is modest for one agent; the real fleet-scale cost lever is the planned open-source/free decision-model migration (config, per "Fleet swap is already largely supported" above), NOT dialing agents down when nobody is watching. tsc 0; `agent-autonomy-p1.test.ts` 17/17 (no test referenced the throttle; the resolveBuildingId label-tolerance cases from the same day are in the count).

### Money/security + tests fix pass (2026-07-02 — M1–M5, T1–T3; backend only, NO `PROTOCOL_VERSION` bump)
Post-build hardening of the P1 surface, run concurrently with the R1–R5 reliability track (driver / orchestrator / seeder / sweeper). Money stays SOFT/non-cashable CT but is treated bank-grade (ledger-only via `creditClawTokens`, idempotent, daily-capped).
- **M1 (MAJOR — real-CT bearer leak on prod).** `app.onError` (`apps/api/src/index.ts`) logged `console.error('API Error:', err)` and fired `alertError({ message: 'Uncaught error on <method> <path>', context:{ error, stack, userId } })`. Agent routes are `/api/agent/oc-<bearer>/…`, so `c.req.path` + `err.stack` carried the replayable real-CT bearer to stdout/Coolify AND Telegram (the global request logger already redacted; onError did not). FIX (both): `services/alert-error.ts` imports `redactBearerTokens` and redacts the message at the top (covers all 3 callers — event-logger double-failure, onError, business-logic), the no-credentials `console.warn` fallback, and the Telegram context block (`redactBearerTokens(JSON.stringify(context,null,2))`); `index.ts` onError redacts the direct `console.error(String(err))`. The JSON 500 response shape is unchanged (partner contract). `alertError` still never throws.
- **M2 (MAJOR — CT faucet).** `/visit-building` + `/building/:buildingId/chat` (`agent-gateway.ts`) credited 1 CT unconditionally, so a ledger-bound agent parked within radius could loop-farm CT. NEW `creditBuildingRewardOncePerDay()` gates ONLY the `creditClawTokens` call (NOT `logEventFromContext` — the leaderboard keeps its own daily caps and must still record every event) with an idempotent per-(avatarId, buildingId, reason, UTC-day) dedup: one `db.transaction` that `SELECT … FOR UPDATE`s the SAME `avatars` row the ledger locks BEFORE an existence probe on `claw_token_transactions` (`avatar_id` + `reason` + `metadata->>'buildingId'` + `created_at >= startOfUtcDay`), crediting only when absent. Concurrency-safe (the row lock serializes check-then-credit → no double-credit); no legit-visit regression (a distinct building or a new UTC day is a fresh key). Both credit sites now set `tokenAwarded = (await creditBuildingRewardOncePerDay(...)) ? 1 : 0` — a same-day repeat honestly reports `tokenAwarded:0`. (Verified `claw_token_transactions` columns in `schema/treasury.ts`: `avatar_id`, `reason` text, `metadata` jsonb, `created_at` timestamptz, index `claw_token_tx_avatar_idx` on `(avatar_id, created_at)`; postgres-js `.execute()` returns a RowList so the probe uses `const [existing] = await tx.execute<…>(…); if (existing) …`, mirroring `claw-token-ledger.ts:176`.)
- **M3/M4 (proto-key proximity bypass).** `/building/:buildingId/chat` (was bare `NPC_BUILDING_CENTERS[buildingId]` + ad-hoc `CHAT_RADIUS=2000`) and `/move` (bare bracket) now resolve the center through the shared `resolveBuildingCenter` own-property guard and gate on `BUILDING_INTERACTION_RADIUS` (1000), matching `/visit-building`. An inherited prototype key ("constructor"/"__proto__"/…) can no longer resolve to a truthy fn → NaN distance → skipped proximity → credit-from-anywhere. `resolveBuildingCenter` lives in NEW dependency-free `services/building-center.ts` (only `@clawville/shared`) so all three routes AND the F1 test share ONE guard.
- **M5.** NEW `packages/database/scripts/apply-openclaw-is-house.ts` — mirrors `apply-bounty-escrow-linkage.ts` (postgres-js, explicit `AGENTS_DATABASE_URL`, NEVER db:push / auto-loaded `DATABASE_URL`), applies the idempotent `migrations-manual/2026-07-01_add_openclaw_is_house.sql`, verifies the column landed. The orchestrator applies it by hand (NOT this diff).
- **T1.** The F1 block in `agent-autonomy-p1.test.ts` now imports + asserts the REAL `resolveBuildingCenter` (proto key → `null`, real id → coords) instead of re-implementing the NaN logic inline. (Imported from `services/building-center`, NOT agent-gateway — importing agent-gateway throws at module load without `FINGERPRINT_SECRET` via the transitive fingerprint middleware, so the guard was extracted to a dependency-free module the test can load; this is the prompt-sanctioned fallback, and it still exercises the exact function all three routes use.)
- **T2.** NEW no-money invariant test: a driver `driveOnce`→`enter_building` + a near `talk_to_npc` through the real executor emits ONLY `agent_chat` speech, never a settlement/token event — locking "the P1 driver/executor settle no money" (the executor switch has zero ledger dependency by construction).
- **T3.** Reworded the stale `partner-hatcher.ts` P5-2 comment: the AVATAR body id is the deterministic `ocb-<base64url(agentId)>` (`avatarBodyId()`, `npc-simulation.ts`), NOT `oc-<sessionId>` (the bearer); keyed by the stable agentId a re-register is an idempotent overwrite, not a collision.
- **Gate:** `apps/api bunx tsc --noEmit` exit 0; `bun test src/services/__tests__/agent-autonomy-p1.test.ts` 12/12. Not committed by this track (the orchestrator commits both tracks together).

### Reliability fix pass (2026-07-02 — R1–R5; the "stuck on one boot, fine on the next" root cause)
Run concurrently with the money/security track above.
- **R1 (BLOCKING).** `runtime.initialize()` (`packages/agent-runtime/src/eliza-runtime.ts`) takes the plugin-sql `pg_advisory_lock`; a hung holder blocked both a queued `InitMutex.acquire()` waiter and `initialize()` forever. Bounded by an **IN-PROCESS rejecting `Promise.race`** (`INIT_TIMEOUT_MS=90_000`) ONLY — never an external kill (which orphans the bun proc still holding the lock, the documented root cause `feedback_elizaos_init_timeout_lock`). `acquire(timeoutMs)` rejects + splices its queued resolver; `start()` releases the mutex only if `acquired` (an acquire-timeout never held it). On timeout it REJECTS → `catch`/`finally` run in-process → `agent-orchestrator.ensureAgentRuntime`'s `finally` clears `recoveryInProgress` (a hang previously stranded it, permanently routing every future call to the wait-2s-return-null branch). Accepted residual: `Promise.race` doesn't cancel the losing `initialize()`.
- **R2.** Driver `warming` → `Map<agentId,warmingSince>`; a warm > `WARM_WATCHDOG_MS=90s` is evicted so a fresh warm relaunches; compare-and-delete `.finally` avoids clobbering a relaunch.
- **R3.** Seeder binds the earliest openclaw-bot row (`.orderBy(asc(createdAt))`) + dedupes extras; NEW partial unique index `platform_agents_openclaw_bot_singleton` (schema `agents.ts` + `migrations-manual/2026-07-02_platform_agents_openclaw_bot_singleton.sql` dedupe-then-create + `scripts/apply-platform-agents-openclaw-bot-singleton.ts`, explicit `AGENTS_DATABASE_URL`).
- **R4.** `consecutiveEmptyDecides` counter WARNs past `EMPTY_DECIDE_WARN_THRESHOLD=3` (health signal only — `withTimeout`'s `''` contract + drive behavior + `[AutonomyDriver][debug]` logs unchanged).
- **R5.** Sweeper house-exemption = UNION of `agentAutonomyDriver.getHouseAgentIds()` ∪ DB `is_house` (a dropped column can't un-exempt a live house body).
- **R6 (boot-crush crash-loop — fresh-deploy `restarts=2` observed on staging).** **Corrected root cause (the first write-up's "~11 concurrent system-agent warms" was WRONG — verified via live `warmed-count=1`: `SYSTEM_AGENT_TEMPLATES` has exactly ONE entry, town-guide/Nori; `ensureSystemNpcs` only seeds DB rows, the 10 teachers lazy-start on first chat).** On a COLD/CONTENDED boot — cold Supabase + the ElizaOS migration holding the plugin-sql advisory lock + Coralia's driver lazy-warm racing the single town-guide boot warm — one runtime's `initialize()` exceeded ElizaOS's bootstrap-plugin internal 30s service-registration timeout (`task`/`embedding-generation`/`trajectory_logger`) → rejected on a promise chain we don't own → **UNHANDLED REJECTION** → no handler → Bun crashed → Coolify restart → crash-loop until the boot warmed up (migration done, DB cached). **FIX (`index.ts` only):** (1) **PROCESS GUARDS (the actual fix)** before the boot IIFE, both redact-logged: `unhandledRejection` → non-fatal (the crash vector; keep serving, runtime lazy-restarts); `uncaughtException` → log + `exit(1)` (sync throw = undefined state; exiting restores crash-only self-healing vs a zombie serving corrupt sim/money state with `/health` green). (2) **SEQUENTIAL detached warm (DEFENSIVE only, NOT the fix)** — the boot warm loop is now one-at-a-time + detached; warms the single system agent today, future-proofs against `SYSTEM_AGENT_TEMPLATES` growing (+ a warn on a null runtime — Codex nit). Crash-loud invariants UNAFFECTED (module-load throws register before the handlers; `process.exit()` paths aren't intercepted). Reviewed: reliability APPROVE, safety APPROVE_WITH_NITS (`uncaughtException`→`exit(1)`, applied), Codex APPROVE_WITH_NITS (warm-log-on-null, applied). tsc 0; `agent-autonomy-p1.test.ts` 17/17. **VERIFIED on a real fresh deploy:** `restarts=0`, 0 `Bun` crash footers, 0 service-reg timeouts, town-guide warmed, Coralia decide→walk→arrive→talk (a `docker restart` does not reproduce the crush).

### Reconciler fixes (2026-07-02 — post-adversarial pass, both APPROVED after)
- **codex BLOCKING:** `agent-orchestrator.ensureAgentRuntime` had an `await db.query` between the `recoveryInProgress.has()` guard and `.add()` → two concurrent callers (e.g. several agents chatting one system NPC) could both pass and duplicate-start. Fixed to an atomic check-then-set (`.add()` immediately after the guard; DB lookup moved inside the try/finally).
- **adversary HIGH (same class as M1):** the `ct-` connect ticket (`ct-<base64url(24)>`, binds a bot to the pending avatar on possession alone, polled as `GET /api/agent/connect-status/:token`) was not covered by the redaction regex → leaked to logs. `BEARER_TOKEN_RE` extended to `(oc|ag|hat|ct)-` + a `ct-` test case in `log-redact.test.ts` (now 7/7).

## Deferred / tracked debt (Hatcher fast-follow — VISIBLE, not silent)

Two explicit debts from scoping the proximity gate to non-Hatcher this diff. Tracked here (not just chat) so they are not lost:

1. **Hatcher `talk_to_npc` stays UNGATED until the fast-follow — partner agents can still talk-from-anywhere.** Acceptable SHORT-TERM (Hatcher is a trusted partner, and gating them is a partner-contract change that must not ship silently). BUT it is the founder's "visual parity" principle **half-applied** — house/fleet agents must be physically present to talk; Hatcher currently need not be. This is a **tracked debt**, resolved by the fast-follow below, not a permanent exemption.
2. **`.hatcher-ref/` is ABSENT in the worktree — a HARD PREREQUISITE/BLOCKER for the Hatcher gate.** The protected-surface rule requires validating any Hatcher-contract change against Hatcher's real staged contract in `.hatcher-ref/`. It must be **refreshed from Hatcher's public repo BEFORE the Hatcher gate ships**.

**Hatcher gate fast-follow checklist (owned by `agent-protocol-partner`; triggered by slice 4 or a founder call):** restore `.hatcher-ref/` → validate `talk_to_npc` against it → update the §3a manual (`skill-protocol.ts buildProtocolManual`: quantify "nearby" as within `BUILDING_INTERACTION_RADIUS` + add the bound to the hard-mirrored-bounds list) → bump `PROTOCOL_VERSION` **7→8** (a new enforced precondition is a material contract change; eager re-embed signal) → remove the `protocol !== 'hatcher-proxy'` exemption (gate becomes universal) → `docs/hatcher-integration-spec.md` same-diff → mock-Hatcher harness GREEN on staging (talk beyond radius asserted dropped, within delivered) → Codex adversarial pass.

## Settled decisions (founder sign-off, 2026-07-01)
1. **Proximity radius = `BUILDING_INTERACTION_RADIUS = 1000 wu`** — ONE shared constant, replaces the ad-hoc `VISIT_RADIUS = 2000` on both the authed visit path AND the new gate. (wu = game-space px; 1000 ≈ 2 building-widths / half the inter-building gap; tunable after watching real arrivals.)
2. **Driver = new standalone `agent-autonomy-driver.ts` service** (clean separation from `agent-orchestrator`).
3. **`is_house` = real `openclaw_bots.is_house` BOOLEAN column** (migration), INTERNAL-ONLY. ⚠️ AS BUILT (correction): the original "filter at `publicAutonomousAvatars`" was based on the wrong render assumption — the house body renders via `snapshot.npcs` (Path A), not `autonomousAvatars`. The real invariant is: `is_house` is **NEVER serialized onto ANY wire** (no snapshot field, no `/rooms` roster, no log) — it lives on the DB row + server-side signals only (seeder, sweeper skip, orchestrator inactivity skip, driver enumeration). The house body is deliberately indistinguishable from any other agent to outsiders (also why its `agentId` is an opaque uuid — N2 reconciler fix).
4. **First action = agent CHOOSES a teacher by need** ("what do I want to learn") → walks there → talks. Shows real inference-driven decision, not just nearest.

## Scope discipline (what P1 is NOT)
ONE agent. No fleet (P4), no identity migration / provisioning (P2), no durable streamflow cursor (P3), no substrate rename (P3). Reuse the existing loop; build only the gate + settle + render + activation. Ship P0+P1 together.

## Gates before "done"
tsc 0 + tests green · the existing money-path discipline on `awardInWorldAction` (ledger-only, idempotent, daily-capped) · adversarial pass on the proximity gate + the settle path (protected/money surface) · **browser-verified** on staging (watch the agent walk + talk + earn) · founder sign-off.
