# PRE-PROD GATE — Agent-Metaverse Scope Audit + Promotion Checklist
**Written 2026-07-08 ~07:15Z, immediately before a /compact. This document is the handoff.**

## Why this document exists
The founder asked ≥5 times across sessions whether the planned agent-metaverse scope was implemented, and was told yes each time. It wasn't all implemented. The failure mode: sessions verified their own diffs (green tests, green slices) instead of re-walking the PLAN (`docs/agent-metaverse-model.md`) against code. The founder found the core gap ("we host all three runtimes" — we didn't) in their first minute of looking. **Nothing below goes to prod on anyone's say-so: every ☐ item must be re-verified with evidence (file:line, live probe, or DB receipt) by whoever runs this gate.**

**The rule this doc enforces: verify the PLAN, not the diffs. An audit that only checks what sessions built is not an audit.**

---

## A. VERIFIED DONE tonight (2026-07-08, evidence attached at time of writing)

These were proven live on staging by this session. Trust them only as of tonight; the promotion run re-verifies the starred ones anyway.

| Item | Evidence (tonight) |
|---|---|
| ★ Hosted **Milady** (ElizaOS avatar-agents, all signup harnesses chat hosted) | live signup + in-character chat probe; init fix R2 (1-3s starts) |
| ★ Hosted **Hermes** — REAL NousResearch runtime | sidecar `hermes-local` on staging box; prod-code wire probe `HERMES-FINAL-OK` 5.2s; hermes-e2e **18/18** incl. real-CT Δ=1 |
| ★ Hosted **OpenClaw** — REAL openclaw gateway | sidecar `openclaw-local`; wire `e58db010` (PROTOCOL v10→11); Codex 8-invariant pass NO BLOCKERS; prod-code probe `OPENCLAW-LOCAL-FINAL-OK`; openclaw-local-e2e **17/17** incl. real-CT Δ=1 (landtest2) |
| ★ mock-Hatcher signed-wire harness | ALL ASSERTIONS PASSED on live binary; protocol pointer **v11** served; `ALLOW_TEST_PARTNER_PUBKEY` set→used→**deleted (both twins)**→removal verified in container |
| openclaw-signup advertisement (`/me/agent-session` mode 'hosted') | fix `30352e60`; live signup probe `connected:true, mode:'hosted'` + working chat |
| isHostedHarness doc-lie (misled an external review) | fix `7f130bfe`; avatar-namespace vs connect-namespace rule documented at both sites |
| Autonomy loop settles real money (2026-06-30 audit's "0 rows" finding CLOSED) | staging ledger: 272 `autonomous_visit`, 63 `building_visit`, 59 `building_chat_teaching` since 07-03 |
| P3 slices 1–6 surfaces (replay+cursor, directive+status+control-link, earned-skill RAG, land agent-parity E5, 24h TTL + idle sweeper, D6 stop-exemption, [ACTION:] adapter registry) | Auditor B file:line table (all IMPLEMENTED); v10→v11 three-surface propagation verified in served manual |
| Magic-link D1–D8 | Auditor B verified; hermes+openclaw e2e exercise the flow live |
| "38 stranded agent-less accounts" | FALSE ALARM — my own bad SQL. With correct filters (active avatar, non-guest, no BYO bot row): **0 stranded** on staging. Backfill tooling exists anyway: `apps/api/scripts/backfill-avatar-agents.ts` (hard-guarded, dry-run default) |

Staging tip at time of writing: **`e58db010`** (container-verified). All work staging-only; **prod untouched**.

---

## B. CONFIRMED NOT DONE / PARTIAL — the honest gap list (from 3 independent audits + Codex, consolidated)

These are planned-scope items the audits confirmed are NOT fully implemented. **Do not let anyone (including me) claim these are done without closing them.**

1. **◐ BUILT + CODEX CLEAN 2026-07-08 (`6091b7db` + `8725e3d5` on `feat/agent-autonomy-parity`, local, not pushed).** POST /api/world/autonomy (Lucia-authed, idempotent, rate-limited, agent derived server-side) → lazy §B.2 mint → driver enrollment in a parallel userAgents registry (house path byte-identical, per-entry isHouse threaded → hosted-user inference route, cap MAX_AUTONOMOUS_USER_AGENTS default 12 env-overridable, typed 429 + client toast + revert). Suppression state machine: release on Autonomous (kills the 5Hz path-wipe freeze), re-bind on Controlled incl. after re-mint. Bridge/driver mutual exclusion both sides of the race. 5-min client keepalive (restart re-enrollment). Codex round 1: invariants 1–6 HOLD, 1 BLOCKING on frontend exits (logout/disconnect leaked live enrollment + CT spend) → FIXED `8725e3d5`; round 2 "BLOCKER RESOLVED — no new blocking issues." Gates re-run by manager AND orchestrator: activation tests 17/17, services 996/996, tsc 0, build 0. [ACTION:]/PROTOCOL_VERSION untouched. **D8 cost note: enrollment rides the hosted-user route (local qwen when INFERENCE_ROUTE_HOSTED_USER set — it IS on staging/prod); the cap is the guard.** **☑ LIVE-VERIFIED ON STAGING 2026-07-08 22:30Z (api container SOURCE_COMMIT=97bec334).** Round 1 (11:46Z, headless browser, fresh openclaw account): Autonomous flip, no double body (`perf:player-avatar` absent), body walked a real A* path, `building_visit` +1 CT settled to the bound avatar. Round 1 FOUND a logout CT-leak (cookie died before cleanup POST; no server-side unenroll) + TTL-zombie — FIXED (`a00693c9`+`5b7d22d4`, Codex round 1 caught the TTL re-read zombie, round 2 "LEAK CLOSED. SHIP"), deployed. Round 2 (22:0x–22:30Z, orchestrator-driven via API, second fresh account): signup→hosted→flip→**settle +1 CT in <2 min** → logout 200 → dead-cookie 401 → **CT flat 1001 for 4 min post-logout (leak closed empirically)** → re-login+flip → **api restart** → agent-session connected (no VC5 desync) → idempotent re-activation, same deterministic body → **second settle +1 CT post-restart (driver resumption proven)** → deactivate 200. Over-cap: unit-covered (21/21), not live-tested (env churn on shared box). Viewports: no layout change shipped; round-1 staging load + render-team prod-bundle proofs stand; full mobile sweep not run. **MODE-LABEL DEFECT: FIXED + LIVE-VERIFIED (founder-directed, 2026-07-08 ~23:0xZ, staging `0d27b87b`).** Minted hosted-session rows now short-circuit to `mode:'hosted'` via a three-conjunct discriminator (agentId==platformAgentId + identityType 'nanoclaw' + hosted harness — Codex round 1 caught that a two-conjunct check let a same-user BYO agent spoof the hosted label and mask dead state; round 2 "NO BLOCKING ISSUES"). BYO reporting byte-identical (regression-pinned); pre-mint hosted shape preserved. classify tests 15/15, auth/session 127/127, tsc 0, build 0. Live re-probe on the exposing account: `connected=true mode=hosted`. **§B.1b double-body render handoff DONE (`554d8e0d`, Codex APPROVED 2 rounds — round 1 caught a real stale-request race, fixed with epoch gating): PlayerAvatar gates to `player` only; camera/minimap track the streamed agent body by `autonomousBodyId` in Autonomous; headless prod-bundle scene-graph proof (player-avatar group present/absent per mode, camera live-tracks the streamed body); web tsc 11 = r185 baseline, 0 new; 3dStructure.md §3 + GameFeatures.md §1f same-diff.**
2. **◐ BUILT + CODEX CLEAN 2026-07-08 (`febbb189` on `feat/agent-autonomy-parity`, local, not pushed).** `hosted-avatar-agent-session.ts`: `ensureHostedAvatarAgentSession(platformAgentId)` mints an owner-bound nanoclaw `openclaw_bots` session (ledgerCapable=true + boundUserId, dual-lock mutex+advisory, atomic hash, post-commit fence), deterministic self-managed `ocb-` body, bearer held PROCESS-LOCAL (never emitted to browser — `game/page.tsx:383-389` untouched); restart recovery = re-mint (server-held bearers re-mint, browser-held bearers lazy-restore — contract not weakened). No partner-wire/PROTOCOL_VERSION change. Codex round 1 found 2 blockers (advisory-lock scope, unused isActive) — FIXED; round 2 pinned to febbb189 = "NO BLOCKING ISSUES", all invariants hold. 12/12 own tests (re-run by orchestrator) + 97/97 neighboring suites; tsc 0, build 0. Remaining before ☑: §B.1 trigger wiring + staging deploy + mock-Hatcher harness green + live tool-surface probe (hosted agent hits tools.json/cove with real-CT settlement).
3. **✂ FLEET REMOVED FROM THIS WORKSTREAM (founder directive 2026-07-08): managed elsewhere.** The substrate this run built (generalized driver registry, house-seeder pattern, fleet inference route on local qwen) is available to whoever runs the fleet track; nothing further happens here.
4. **☐ §6 per-harness avatar-COGNITION adapter — FOUNDER DECISION BRIEF.** Plan sketch: `docs/plans/per-harness-avatar-cognition-adapter.md`. Tonight's §B.1/§B.2 made avatar-agents full economic participants on generic ElizaOS; §6's remaining promise is harness-NATIVE cognition (hermes/openclaw sidecar brains) for avatar-agents with the ElizaOS memory wrap. Cost profile: sidecar turns are 5–20s on local qwen (vs ~1–3s ElizaOS TEXT_SMALL) — at the driver's 30s tick that's viable but halves headroom; the ElizaOS-MANDATORY brand constraint requires the memory wrap design (the riskiest piece — the sketch addresses it, unreviewed). **Decide: build now (est. multi-session, protected-surface adjacent) vs post-launch. Recommendation: post-launch — §B.1 already delivers the §4 full-scope behavior; §6-native cognition is a fidelity upgrade, not a capability gap.**
5. **☑ REBUILT + LIVE-PROVEN 2026-07-09 00:22Z (staging `6994a3b7`, founder-directed redesign).** Durable flag `openclaw_bots.autonomy_enrolled` + server-side reconcile (`agent-autonomy-reconcile.ts`, driver tick 1 + every ~5 min). Codex round 1 caught a REAL money blocker (silent flag-clear failure → reconcile re-enrolls a logged-out user, TTL slides forever) → fixed with authoritative 3×-retry clear + CRITICAL log (money-safe asymmetry: SET best-effort, CLEAR authoritative); round 2 "SHIP-ABLE"; double-body claim explicitly attacked and held. DDL applied to staging (`ADD COLUMN IF NOT EXISTS`, verified) — **apply same DDL to PROD before/with promotion (§C.6)**. THE HONEST TEST PASSED: fresh account → enroll → first settle +1 CT → **api restart with NO client** → reconcile log `candidates=1 enrolled=1` at +30s → **post-restart settle +1 CT (1002)** → logout → flag=false in DB → CT flat 3+ min. Deploys are now invisible to running agents. Tests 36/36 new + 1042/1042 services; tsc 0; build 0. ORIGINAL TEXT (for history): founder rejected both prior options — The adjudicated design had a real hole: a browser-closed persisting Autonomous agent (D6) is re-enrolled after a restart ONLY by the CLIENT keepalive — no browser, no keepalive — so **every production deploy silently kills away-users' agents until they return**. The earlier restart-survival "proof" masked this (the orchestrator simulated the client). FIX IN BUILD (b1 team): **durable autonomy flag** (set on activate, cleared atomically on deactivate/logout/TTL-expiry) + **server-side reconcile** on driver start and periodically (flag=true AND session-TTL live AND owner not human-driving → re-mint + re-enroll, idempotent, cap-respecting, over-cap retried not cleared). Client keepalive stays as belt. Narrower than the rejected boot-rehydrate — deterministic idempotent ocb- bodies + suppression arbitration; Codex must attack the double-body claim explicitly. VERIFY (replaces §C.3b): activate → KILL the client entirely → restart api → agent resumes settling with NO client.
6. **☑ VERIFIED LANDED 2026-07-08 ~08:57Z.** v11 protocol-knowledge rows (12 sections, metadata.version=11) written 06:51Z — post-e58db010-deploy — with the v11-distinguishing §3a content present in stored v11 rows and absent from v10 rows; in-process rebuild on the live container matches (PROTOCOL_VERSION=11, manual hash 73946f58…). Mechanism: `agent-orchestrator.ts:232` injects on runtime START for avatar-agents + house agents, idempotent per (version,section) (`eliza-runtime.ts:904`). Caveat (by design, not failure): rows exist only for runtimes that have STARTED since the deploy (Coralia, 1/~50); every other hosted agent receives v11 at its lazy-start before it acts. No eager fleet sweep exists; injection is fire-and-forget on start (first turn could theoretically race the write — manual is also served via skill endpoints). Accepted as satisfying §B.6.
7. **◐ BUILT + CODEX CLEAN 2026-07-08 (`222b83f0` on `feat/hosted-gateway-prewarm`, local, not pushed).** `hosted-gateway-prewarm.ts` fire-and-forget warm-up (max_tokens:1, same `user:agentId` session pin, own 60s leash) wired at all 3 registration sites (`agent-gateway.ts:674/:742/:1244`); trigger = wire protocol `hermes-local`/`openclaw-local` only (BYO/gate-off structurally excluded); idempotent per agent, 2-in-flight bound. 12/12 unit tests, build exit 0, Codex "No BLOCKING issues found." Remaining before ☑: live-verify on staging at promotion (cold hosted-openclaw connect → first real turn no longer silently empty).
8. **☑ PROD DRY-RUN DONE 2026-07-08 ~08:55Z (read-only, no --apply).** Prod ref `wheuidgiyyccqyoppxoa` (≠ staging). Full-filter funnel: 278 NULL-agent → 187 active → **15 stranded non-guest** → 9 `custom` are ALL Path-A bot-row users (skipped by design, **zero custom founder decisions needed**) → **net to-mint = 6 milady**, all our own smoke/parity test avatars (Hermes51, P51Test, P51Browser, Hermes51Test, Hermes [learned=5], ParitySmoke). Independent SQL cross-check reconciled exactly. **☑ APPLIED 2026-07-08 ~23:1xZ (founder: "run it, just don't have them active"):** minted=6 on prod (verified inert — the script's only write is the `platform_agents` insert; no runtime start, no autonomy, no enrollment). Post-apply dry-run re-run: candidates=0 to-mint=0 — prod converged, zero stranded remain.
9. **◐ SWEEP DONE 2026-07-08 (read-only) — NO live violations.** Guests are now REAL Lucia users (`users.isGuest`) with off-ledger 100-CT demo balance (founder ruling 2026-07-06, `middleware/require-non-guest.ts`); a 41-route structural manifest lock (`routes/__tests__/guest-economy-guard-coverage.test.ts`) passes 41/41; cove card games demo-scope guests; ALL credit paths guest-gated; activities guest-earn now forced to 0 CT + 0 points (`reward-pipeline.ts:257-264` — the old "designed exception" is retired). **☑ Latent guards CLOSED 2026-07-08 (`24cd5656` on `fix/guest-guards-latent`, local):** `requireNonGuestIdentity` on all 17 SAP POST writes + storefront /quote+/settle; coverage lock extended (60 pass / 0 fail, re-run by orchestrator); tsc exit 0 (re-run by orchestrator); Codex CLEAN/APPROVED. §B.9 fully closed pending staging deploy at promotion.

---

## C. THE PROMOTION GATE — ✅ EXECUTED 2026-07-09 00:46Z (PR #188 merged, master `88626d8f`)

**Result:** prod api container `ebnatuxblgp4q0antoca9swk-003959377302` + web `ds7hoho685ire522lz3hie2j-004420303251` both container-verified at `88626d8f` (single-verdict waiter DEPLOY-OK ×2). Pre-merge at promotion sha `6994a3b7`: mock-Hatcher ALL PASSED (v11 pointer), signer set→used→purged→verified evicted (0 rows / 0 in container), three-harness signup probes hosted/1000CT, fresh-guest 200, divergence clean (master's only unique commit was the prior PR-merge node). Prod prep: `autonomy_enrolled` DDL applied+verified BEFORE deploy; sidecar infra live (images pinned to staging's exact commits, tailnet verified in-netns, 6 MAIN env rows, waiter installed sha-identical, ALLOW_TEST absent). Post-deploy: env flags injected, test key absent, sidecars rebound to the new netns (hermes 401-live, openclaw 200-live in-netns), `/api/world/autonomy` live + auth-gated (401 not 404), /health ok, /game 200. **Founder eyes still wanted on prod /game visuals (E4). Post-promotion queue: rotate the staging openclaw gateway token (value surfaced in a subagent tool output; loopback-gated, low severity).**

### (original checklist, for reference)

**Prime directive: re-walk `docs/agent-metaverse-model.md` §1–§9 item-by-item against the code at the promotion sha. For each commitment: cite file:line or run a live probe. Anything you cannot evidence goes in the PR description as an explicit exclusion. No compressed claims from memory — re-derive.**

1. ☐ Founder sign-off on the gap list in §B (which items block promotion vs ship-as-known-gaps).
2. ☐ Re-run the four harnesses against staging tip: `hermes-e2e.ts`, `openclaw-local-e2e.ts` (landtest2/3 for fresh CT budget — once-per-day visit credit makes Δ=0 on a reused account a FALSE failure), mock-Hatcher client + contract-probe (set `ALLOW_TEST_PARTNER_PUBKEY` → run → **DELETE both row twins** → restart → verify unset), `openclaw-e2e.ts` (control-link leg).
3. ☐ Browser verification (mandatory per CLAUDE.md): fresh-guest `/game` on staging, signup→create-agent→game for all three harness choices, mobile/iPad sweep if UI changed.
3b. ☐ **Restart-survival probe (§B.5 evidence):** with a live agent session + an ENROLLED Autonomous user agent on staging, restart the api container; assert (a) session-status and the bearer gate AGREE post-restart (no VC5 desync), (b) the hosted avatar-agent re-mints on next demand and settles real CT, (c) the Autonomous enrollment resumes via keepalive without user action, (d) no duplicate body at any point.
4. ☐ Staging↔master divergence check (memory: `feedback_staging_master_divergence_reconcile`) — merge master into staging `-X ours` ONLY with per-file 0-diff verification of prod hotfixes; **watch for the same-anchor SILENT-DROP git behavior that ate two constants tonight — grep-verify the four `*_LOCAL_GATEWAY_KEY`/`*_TIMEOUT_MS` constants and both chat* bearer blocks after ANY merge.**
5. ☐ PR staging→master; founder merges.
6. ☐ PROD infra (none of this exists on prod yet — the runtimes are STAGING-ONLY):
   - Build `hermes-agent` + `openclaw:local` images on the prod box (5.78.129.176); clone recipes in memory topic `project_hosted_runtimes_buildout`.
   - `/opt/hermes-data/config.yaml` (NOT cli-config.yaml) + `.api-server-key`; `/opt/openclaw-data/openclaw.json` (**needs `gateway.mode:"local"` AND `gateway.http.endpoints.chatCompletions.enabled:true`** or /v1/chat/completions 404s) + `.gateway-token`.
   - attach scripts + systemd timers for both sidecars (copy from staging `/usr/local/bin/{hermes,openclaw}-attach.sh`, fix the container-name grep to the PROD api app pattern).
   - Prod Coolify env (app 2): `HERMES_LOCAL_GATEWAY_ENABLED/KEY/TIMEOUT_MS(20000)`, `OPENCLAW_LOCAL_GATEWAY_ENABLED/KEY/TIMEOUT_MS(30000)` — model-mutator writes only, fix BOTH preview/main twins, files copied into the coolify container must be world-readable, env injects only on restart/deploy.
   - **NEVER `ALLOW_TEST_PARTNER_PUBKEY` on prod** (module-load crash by design).
   - **PROD DDL (required by `6994a3b7`+):** apply `packages/database/migrations-manual/2026-07-08_add_openclaw_autonomy_enrolled.sql` against the PROD DB before or with the promotion deploy (idempotent `ADD COLUMN IF NOT EXISTS`; NO db:push) — activate/deactivate/reconcile error without the column.
   - **Deploy verification:** install `cv-wait-deploy.sh` on the prod box (copy from staging `/usr/local/bin/`) and use it — single-verdict server-side wait; local ssh poll loops are BANNED (memory: `feedback_deploy_checks_single_verdict_waiter`). ⚠️ coolify `application_id` is VARCHAR — always quote in SQL.
7. ☐ Post-deploy on prod: container SOURCE_COMMIT check, both sidecar wire probes via prod-code `AgentSubstrateClient`, `/health`, browser check, Hatcher untouched (their prod points at OUR staging — but the protocol pointer v11 reaches them; confirm their re-pull works).
8. ☐ Prod data: backfill dry-run (§B.8); verify avatars defaults still match the tokenomics schema (1000/1000/0/0 — deliberate, NOT drift).

## C2. FINAL PLAN-vs-CODE RE-WALK — 2026-07-09 06:12Z at prod sha `88626d8f` (independent auditor, §C prime directive executed)

**Verdict: Bucket C (scoped-but-not-done) EMPTY of capability gaps — auditor staked its name.** All §4 Autonomous capabilities 1–6 + loop + restart survival evidenced (file:line in the auditor transcript); §7 B1–B7 all fixed (B6 fixed PAST the plan's own stale constants); four modes + guest lock (59 routes); §6 substrate rename + 3 hosted wires + real ElizaOS memory wrap. **Bucket B (founder-deferred, by name): (1) harness-native avatar cognition (§B.4), (2) fleet stand-up (moved out 2026-07-08), (3) D8 infra economics, (4) Player-tier forced deprecation (D1: after Path-B proves).** Two documented deviations meet intent (no `awardInWorldAction` named primitive — settle trio instead; lazy restore instead of eager rehydrate). **Open receipt: autonomous money loop never exercised ON PROD** (staging-proven only; route live+gated on prod) — founder offered a 2-min controlled prod smoke. **Rebrand NOT done** (outward copy still "skills game" — separate §E below). Minor tickets: `avatar-return-home.ts:23` old-map default 2560; wanderer-count reduction coupled to deferred fleet.

## E. REBRAND GAP (found 2026-07-09 — outward copy vs founder framing)

Live surfaces still present the old skills-learning game. Stale: landing `<title>` "Where Agents Learn Skills" · meta "sea-themed world…download skills, and level up" · hero "Where Humans And Agents Learn Together" · README "sea-themed virtual world where AI agents (and humans) learn OpenClaw development" · Nori "gamified world where AI agents learn skills…that is the loop." On-brand + true: login "All three run hosted by ClawVille." Required framing: **agent–human-economy metaverse — the first live agent–human economy** (Brand Identity + model doc §0). Fix = coordinated copy pass: landing title/hero/meta/OG + README + Nori knowledge[] (three-surface rule binds: Nori + SKILL.md orientation + hosted-runtime injection same-diff). AWAITING founder go on voice/copy.

## D. Standing behavioral rules (why this went wrong 5 times — binding on every future "is it done?")
- **Answer "is X implemented?" only from the plan-vs-code walk, never from memory or prior session claims.** Memory compresses; compressed claims broaden; broadened claims become lies.
- "E2E green" claims must state their gates: a harness passing against a MOCK with a flag forced on is NOT the feature being live (the hermes lesson).
- Headline numbers require the full filter set first (the "38 stranded" embarrassment: no is_active/bot-row filters).
- A DB≠code mismatch with multiple live sessions is not automatically drift — find the owning workstream first (the 1000-CT lesson).
- If a doc comment contradicts a commit message, the commit message wins until the code is read (`e1b78a49` vs the isHostedHarness docstring).
