# 3D Architect — Knowledge Base

## Game-Dev Skill Suite
- [Game-Dev Skill Suite — applicability + overrides](reference/game-skill-suite.md) — 8 generic game skills (3d-generator/image-generator/audio-generator/aaa-graphics/debug-profiler/game-ui/gameplay-systems/qa-release); per-skill when-to-use + ClawVille hard overrides (Iris Xe bans, VRM pipeline, Next.js+R3F stack, Coolify deploy, useIsMobile hook); game-director NOT in frontmatter.

## Standing Rules (read first)
- [Three-Doc Standing Rule](standing-rules/three-doc-standing-rule.md) — Abide by `3dStructure.md` for all visual/3D decisions; `GameFeatures.md` for gameplay; `ARCHITECTURE.md` for tech stack. Unless the main session tells you to change behavior, do NOT deviate from what these docs specify. Every 3D code change requires a same-diff update to `3dStructure.md` (and a "Last Audited" bump). Live code > doc > CLAUDE.md > memory; memory is advisory only. Set 2026-04-17.
- [Collaborative ultrathink team mandatory for 3D / Blender / long tasks](standing-rules/parallel-ultrathink-team-mandatory.md) — A "team" is multiple agents working SEQUENTIALLY on the SAME concern (Implementer → Auditor → Fix loop), NOT N agents working on N different concerns in parallel. The audit step is the point. Every agent prompt must include "use ultrathink reasoning before writing code". Set + corrected 2026-04-29.

## Land Economy
- [Land parcel for-sale render — merged BufferGeometry per tier (PREMIUM rework)](patterns/land-parcel-merged-render.md) — 180 parcels in 7 draw calls: 5 tier body meshes (pad+corner-posts+top-rails, MeshStandardMaterial) + 1 sign-post + 1 sign-plank (MeshBasicMaterial + per-tier atlas, UV-remapped per-parcel at geometry build time). Key technique: `remapUVsToAtlasRow(geo, rowIndex)` before merge collapses all 180 sign planks to 1 draw. NO InstancedMesh, NO ShaderMaterial, NO drei Text/Billboard. Build verified exit 0. 2026-06-17.

## Casino Slot Drums
- [Per-cell-plane drum wheel — cherry-charm 3D slot drums](solutions/slot-drum-per-cell-plane.md) — 12 PlaneGeometry quads orbiting X-axis at DRUM_RADIUS=1.5wu. One 128×128 symbol texture per face. Back-crossing swap for zero-rebuild animation. Ortho camera left=-8.5/right=8.5. Verified prod df53dd3 2026-05-19.

## Reef Race v2 Performance
- [Scenery InstancedMesh per prop type — ScenerySpawner draw-call collapse](patterns/reef-race-scene.md) — Replace per-instance scene.clone(true) with InstancedMesh per SpawnerDef; extract first Mesh geo+mat from GLB; guard: replace ShaderMaterial with MeshStandardMaterial to avoid Iris Xe silent crash. 104→6 draw calls. (2026-04-29)
- [Ramp wedges merged into single BufferGeometry — 6→1 draw call](patterns/reef-race-scene.md) — buildAllRampsGeo() applies per-ramp transform matrix to wedge template, mergeGeometries all 6 at module scope. 6→1 draw call. (2026-04-29)
- [GLB pivot anchor — non-identity node transforms](patterns/glb-pivot-anchor-non-identity-nodes.md) — M_instance × M_pivot × M_child with workGroup identity trick; per-material merge for texture preservation. Shipped on c1fc3c82 (ramps.tsx GLB integration). (2026-04-30)

## Reef Race v2 Chase Camera
- [VRM chase-cam framing formula — camera must see full-height rider](patterns/reef-race-scene.md) — With Milady VRM at world Y≈219 (KART_SCALE×VRM_RIDER_LOCAL_SCALE+offset), CAMERA_OFFSET.y=280 and CAMERA_LOOK_OFFSET.y=130 keeps head 89wu above lookAt; atan2(89,350)=14° within ±30° FOV. Previously 200/80 cut the head. (2026-04-29 QA S1 fix)
- [REST reconnect route must use same sim dispatcher as WS hub](solutions/reef-race-rest-sim-dispatch.md) — activities.ts GET rooms/:id was hardcoded to reefRaceSim, causing reconnect to use v1 ellipse sim even when REEF_RACE_USE_SPLINE=true. Fix: add same reefRaceImpl dispatcher inline. (2026-04-29 QA S2 fix)

## VRM (load-bearing — read first when touching avatars)
- [useVRMInstance(path, instanceId) — canonical VRM loading hook](patterns/vrm-per-instance-cache.md) — Two-tier cache: shared `VRM_BYTES` + per-instance `VRM_INSTANCES`. Replaces scene.clone() of shared vrm. Consumers must call `disposeVRMInstance` on unmount. (2026-04-28)
- [Two visible VRMs sharing one parsed instance corrupts animation — Codex Critical #1](gotchas/vrm-shared-instance-corruption.md) — Cloning `vrm.scene` is wrong; humanoid/springBoneManager/lookAt point at original bones. Fix: per-instance VRM via useVRMInstance. NEVER use React's `useId()` as the key (causes Suspense loops). (2026-04-28)
- [VRM bald-spot is asset-level — runtime patches are dead ends](gotchas/vrm-spring-bone-bald-spot-at-scale.md) — Hairmodel/Hatmodel were authored as plain Mesh under Head scene node; skinning matrices don't propagate. Shipped fix: `scripts/bake-vrm-hair.mjs` rebakes to SkinnedMesh weighted to mixamorig:Head. 7+ runtime attempts failed (spring-bone tune, position offset, headBone.attach, scalp cap, geometry tilt) — all listed inside. (2026-04-28, commit c2b7cd5)
- [Object3D.attach(headBone, hairMesh) puts hair in wrong transform space](gotchas/vrm-runtime-reparent-wrong-space.md) — preserves world transform at moment of attach; baseline becomes wrong as soon as head animates. (2026-04-28)
- [Hand-baking VRM via Blender raw export produces corrupted GLB](gotchas/blender-hand-bake-vrm-corruption.md) — drops VRM extensions, breaks meshopt, may re-encode WebP → `Malformed buffer data: -1`. Use `@gltf-transform/core` + meshopt extension instead. Validate with `scripts/validate-vrm-load.mjs`. (2026-04-28)

## Gotchas
- [Raycaster skips back-face hits (FrontSide) — low anchor at base-platform height misses vertical walls](gotchas/raycaster-frontside-backface-skip-occlusion.md) — `BUBBLE_Y=20` placed anchor in base zone where geometry is flat (+Y normals); ray from above hits back face → skipped; fix: raise anchor to Y≥150 so ray hits vertical wall front faces; NPC name tag at Y≈100 worked, bubble at Y=20 didn't.
- [CylinderGeometry slot reel — camera inside cylinder when radius computed from strip circumference](gotchas/cylinder-reel-inside-camera-unit-math.md) — STRIP_LEN=84, CELL_WU=1.0 → radius 13.37wu; camera at z=6 is inside; fix: PlaneGeometry + UV scroll; offset.y = 1-(p+1.5)/STRIP_LEN; fov 65, z=5
- [GLB material purple/pink when scene.clone(true) used across separate renderer contexts](gotchas/glb-material-purple-cross-renderer-context.md) — `useGLTF` cache shares material references; different Canvas = different WebGL context → purple fallback; fix: clone every material after `scene.clone(true)`
- [GLTFLoader node names are verbatim — no sanitization](gotchas/gltfloader-node-names-verbatim-not-sanitized.md) — Three.js preserves apostrophes, spaces, colons exactly as authored. `getObjectByName("Squidward_s_House")` never finds `"Squidward's House"`. Surfaced: childScaleOverrides + bodyAnchorChild were silent no-ops for 10+ commits. (2026-05-18)
- [GLB node names — CDP scene traversal is the ONLY reliable source of truth](gotchas/glb-name-conventions-cdp-ground-truth.md) — Hex-dump of GLB JSON chunk is unreliable (editor quote-substitution, meshopt re-encode). CDP `window.__W3D.scene.getObjectByName(...)` is ground truth. ClawVille pattern: spaces→underscores, U+0027 straight apostrophe. Exceptions: `SpongebobsHouse` (different artist). Always verify via CDP before writing any childScaleOverrides/bodyAnchorChild key. (2026-05-21)
- [matrixAutoUpdate=false before R3F scale prop — model renders at native micro-scale](gotchas/matrixautoupdate-false-before-r3f-scale-prop.md) — traverse sets autoUpdate=false on scene root BEFORE <primitive scale={n}> prop is applied; Three.js never recomputes matrix; model is invisible; fix: apply scale directly to cloned root + updateMatrixWorld BEFORE the traverse, then NO scale prop on <primitive>. (2026-05-18)
- [R3F mesh with matrixAutoUpdate=false as JSX prop silently ignores position/rotation — all instances stack at origin](gotchas/r3f-matrixautoupdate-false-strips-position-prop.md) — `matrixAutoUpdate={false}` on `<mesh>` prevents R3F from flushing position/rotation props into the matrix; all N instances render at (0,0,0). Fix: remove the prop for static geometry (9 matrix recalcs/frame is negligible). For true static perf need: use ref+useEffect+updateMatrix(). Surfaced: BuildingPedestal, 9 pedestals stacked at origin. (2026-05-21)
- [Chase camera coupled to avatar yaw causes 45° viewport snap on A/D press](gotchas/chase-camera-yaw-coupling-45-degree-snap.md) — Direct `rotRef.current` in behind-position formula swings viewport with every avatar turn; fix: module-scope `_casinoCamYaw` lerped at 0.05 (vs avatar 0.15), shared by both VRM+GLB branches; shortest-path diff mandatory (2026-05-18)
- [Arrow key e.key.toLowerCase() produces "arrowup" not "w" — no movement](gotchas/arrow-key-ekey-lowercase-mismatch.md) — multi-char e.key values ("ArrowUp") → "arrowup" after toLowerCase; doesn't match "w"; fix: `e.key.length===1 ? e.key.toLowerCase() : e.key`, map arrow variants explicitly to WASD slots (2026-05-18)
- [Browser skips keyup on window blur — strands module-scope key state true](gotchas/keyup-not-fired-on-window-blur.md) — alt-tab / OS focus steal mid-hold → keyup never fires → character walks forever; fix: window.blur + visibilitychange → reset all keys (2026-04-25)
- [WebGPU depth buffer allocated at 300×150 before CSS layout resolves](gotchas/webgpu-depth-buffer-300x150-before-css-layout.md) — async gl factory runs before CSS layout flushes; canvas defaults to 300×150; WebGPURenderer.init() allocates depth buffer at wrong size; setSize() only resizes color — fix: getBoundingClientRect() before new WebGPURenderer() to stamp true dimensions. (2026-04-27)
- [TubeGeometry track invisible from above — use flat ribbon BufferGeometry](gotchas/tubegeometry-invisible-from-above-chase-cam.md) — TubeGeometry is a hollow tube; chase camera above sees only edge-on top faces (nearly invisible). Replace with flat ribbon geo (normals +Y, DoubleSide) for a visible road surface. (2026-04-24)
- [Reef Race fog-fade "black portal" on large track — set material.fog=false + push fog distances](gotchas/reef-race-fog-fade-on-large-track.md) — When track scales up but FOG_NEAR/FOG_FAR don't follow, far half of track fades to fog color ("black portal"). Fix: track+guardrail material.fog=false (always visible); FOG_NEAR=2000, FOG_FAR=4500, CAMERA_FAR=5000. Coral keeps fog=true for depth. (2026-04-26)
- [3D track curve MUST match server sim coordinate system](gotchas/3d-track-must-match-server-sim-coords.md) — Entity positions land off-track if 3D CatmullRom points use different coordinates than the server sim. Derive visual track from server constants, not custom art. (2026-04-24)
- [Velocity-derived facing snaps on knockback — use server entity.rot](gotchas/bumper-velocity-facing-knockback-spaz.md) — atan2(vx,vy) facing re-derives on every collision impulse; server rot field is only updated on player input, immune to knockback. (2026-04-24)
- [Three.js fog uses camera world-space distance — kills ortho top-down scene](gotchas/ortho-camera-fog-world-distance.md) — `sqrt(x²+y²+z²)` camera-to-geometry distance, NOT screen depth; top-down ortho cam at y=1100 is 1140wu from floor — all geometry past FOG_FAR=900 = 100% black. (2026-04-24)
- [Mixamo → VRM retarget requires rest-pose-differential quaternion transform](gotchas/mixamo-retarget-rest-pose-transform.md) — naive clone+rename produces permanent T-pose; must apply `q.premultiply(parentRestWorld).multiply(restRotInv)` per bone; VRM-0 axis flip on even components; mixer rooted at vrm.scene (NOT normalizedHumanBonesRoot). Port of Milady's retargetMixamoGltfToVrm.ts. (2026-04-23)
- [Two three.js instances: NodeMaterial + plain WebGLRenderer = `.replace()` crash](gotchas/two-three-instances-nodemat-webgl-crash.md) — `import * as THREE from 'three/webgpu'` in a file rendered by R3F's default Canvas (WebGLRenderer from plain three) causes per-frame crash flood — `NodeMaterial.vertexShader=undefined` → `WebGLProgram` calls `.replace()` on it. Even if you use NO NodeMaterials, the `extend(THREE as any)` call registers them into R3F's catalogue and they appear in materials from other files. Fix: switch to `import 'three'` for any file in a WebGLRenderer scene. (2026-04-23; second occurrence bumper-shells 2026-04-24)
- [Chrome throttles RAF to 1fps in background tabs — misleads CDP bone probes](gotchas/chrome-background-tab-raf-throttle.md) — `document.hidden=true` causes Three.js render loop to crawl; CDP samples at 2s intervals show same bone value even if animation is working; check `visibilityState` before concluding animation is broken. (2026-04-23)
- [three-vrm v3: AnimationMixer must target normalized bones not raw bones; also Three.js sanitizes GLTF node names](gotchas/vrm-threejs-v3-normalized-bones-for-animation.md) — Two bugs: (1) `getRawBoneNode()` produces permanent T-pose — use `getNormalizedBoneNode()`; (2) `sanitizeNodeName()` removes `:` from `mixamorig:Hips` → `mixamorigHips` with no separator — regex `/^mixamorig[_:]/` misses it; fix: `/^mixamorig[_:]?/` (separator optional). (2026-04-23)
- [SkinnedMesh frustum culling causes disappear-at-close-range](gotchas/skinned-mesh-frustum-cull-close-range.md) — VRM + GLB SkinnedMesh bounding spheres come from bind pose (T-pose); animated geometry extends outside them; Three.js culls the mesh when camera is close/angled. Fix: `c.traverse(o => { o.frustumCulled = false; })` immediately after EVERY `scene.clone(true)` or `SkeletonUtils.clone()` on a rigged GLB — vrm-loader.ts, arena-npcs.tsx, arena-location-npcs.tsx, player-avatar.tsx, quest-npc.tsx, town-guide.tsx, auction-podium.tsx. (2026-04-21; extended to all sites 2026-04-23)
- [NPC_CULL_DIST_SQ too small causes permanent T-pose on far-spawned VRMs](gotchas/npc-cull-dist-too-small-permanent-t-pose.md) — NPCs spawning > cull-dist from world origin get group.visible=false on frame 1; VRM animators never run a tick; permanent T-pose. NPC_CULL_DIST must be ≥ max spawn-origin distance (≈1400wu in ClawVille). Current value: 2000wu. (2026-04-21)
- [Missing module-scope VRM preloads cause T-pose race condition](gotchas/missing-vrm-preloads-t-pose-race.md) — only milady_official_7/8 preloaded; official_2/3/4 cold-loaded on Suspense mount, racing against animator.init(). Fix: preload ALL VRM NPC paths at module scope before any VRMNpcMesh renders. (2026-04-21)
- [drei Html DOM portal ignores parent group.visible](gotchas/drei-html-ignores-parent-visible.md) — setting `group.visible=false` does NOT hide `<Html>` DOM labels; fix: attach `labelRef` to inner div and imperatively set `display:none/flex` in useFrame alongside group.visible toggle (2026-04-23)
- [camForward.y capture causes altitude drift on mouse orbit](gotchas/camera-forward-y-causes-orbit-altitude-drift.md) — capturing camForward.y before XZ zero and feeding it into playerAltitude couples vertical swim to incidental orbit; fix: zero y immediately, drive altitude only from arrowup/arrowdown (2026-04-17)
- [VRM MToon materials break under MeshStandardMaterial color lerp](gotchas/vrm-color-tinting-mtoon-breaks.md) — skip applyColorTint() entirely for VRM avatars; store avatarColor in Zustand but do not apply visually
- [MeshBasicNodeMaterial ignores scene fog — backdrop renders as hard wall](gotchas/meshbasicnodematerial-ignores-fog.md) — backdrop must be past camera.far or have its own opacityNode distance fade; fog does not apply
- [terrainYRef init 0 causes 4-unit spawn float](gotchas/terrain-yref-init-zero-causes-spawn-float.md) — init to -2 (sand floor Y) so avatar spawns flush; 0 causes visible 200ms drift down
- [7+ point lights cause GPU context loss on Iris Xe](gotchas/point-lights-iris-xe-gpu-saturation.md) — each point light = full per-fragment lighting pass; 7 lights + PCF shadow saturated Iris Xe → context lost; budget: hemisphere + 1 dir no-shadow (2026-04-24)
- [InstancedMesh + ShaderMaterial crashes WebGPU](gotchas/webgpu-instancedmesh-shadermaterial.md) — silent crash, zero console errors
- [drei Text/Billboard crashes Intel Iris Xe](gotchas/intel-iris-drei-text-crash.md) — hard GPU crash on integrated graphics
- [Vegetation must use MeshBasicMaterial on WebGPU](gotchas/seaweed-meshbasic-webgpu.md) — ShaderMaterial vertex animation fails silently
- [three-stdlib KTX2Loader.detectSupport() crashes on WebGPU](gotchas/three-stdlib-ktx2loader-webgpu-broken.md) — uses renderer.extensions.has() (WebGL only); use three/addons KTX2Loader instead
- [lobster.glb faces +Z at rotation.y=0 — THIRD REWRITE 2026-04-16 late PM](gotchas/lobster-faces-plus-z-at-rot-zero-empirical.md) — +Z proven by clean side-view screenshot (no orbit); formula: atan2(worldVx,worldVz); DIR_ROTATION: down=0,up=PI,right=PI/2,left=-PI/2,idle=0; +X claim from AM session was WRONG (camera orbited)
- [Camera-relative movement breaks on mobile with OrbitControls](gotchas/camera-relative-movement-breaks-on-mobile-orbitcontrols.md) — touch orbiting accumulates ~180° over 10s, inverts camForward.xz; use camera-relative for keyboard (fixed 2026-04-21), disable OrbitControls touch rotation if mobile inversion recurs
- [holdMs zeroed on release kills charge classifier — always reads 0](gotchas/holdms-reset-on-release-kills-classifier.md) — `holdMs = spaceDown ? holdMs+dt*1000 : 0` zeros holdMs in the same frame as release; classifier reads 0, quick branch always fires; fix: accumulate while held only, reset on new press in `case 'grounded'`
- [Local MAP_WIDTH/MAP_HEIGHT constants cause silent sync bugs](gotchas/local-constants-vs-imports-sync.md) — arena-terrain, arena-npcs, merged-seaweed all had local duplicates; import from tilemap-data.ts
- [Location NPC village center tile coords must match grid center](gotchas/location-npc-village-center-tile-coords.md) — must always be MAP_COLS/2, MAP_ROWS/2; Phase 6.1: (120,120) for the 240×240 grid (was 80,80 on 160×160)
- [applyStationaryIdleAnimation must NOT add Math.PI to rotation.y](gotchas/location-npc-double-pi-facing.md) — inner animGroup gets +PI stacked on outer facingRotY (which already has +PI); NPCs face 180° wrong
- [arena-location-npcs JSDoc said +Z model but code was -Z](gotchas/location-npc-comment-mismatch.md) — comment mismatch fixed 2026-04-13; code was always correct; SpongeBob GLBs assumed -Z
- [NPC terrain-raycast stagger — seed MUST be integer](gotchas/synchronized-terrain-raycasts-spike.md) — idToSeed() returns FLOAT; (frame + float) % N === 0 never fires; must Math.round(seed)
- [useFrame ref mutation does not trigger React re-render](gotchas/useframe-mutation-no-react-rerender.md) — mutating useRef inside useFrame is invisible to React; pair with useState + setTexts([...alive]) to trigger renders
- [useMemo geo/mat passed as props needs explicit useEffect dispose](gotchas/usememo-geo-mat-needs-dispose.md) — R3F only auto-disposes intrinsic attach syntax; geometry={geo} material={mat} props require manual useEffect cleanup
- [Date.now() in render body is stale — bubbles never auto-expire](gotchas/stale-date-now-in-render.md) — Date.now() in JSX is only fresh on re-renders; add a 1s setInterval tick state to force re-renders for expiry checks
- [Box3.setFromObject() inflates bbox on SkinnedMesh scenes](gotchas/skinned-mesh-bbox-inflation.md) — bind-pose world matrix can inflate Y by 60-100x; traverse only non-SkinnedMesh nodes with geometry bbox + matrixWorld instead
- [Building GLB normalization: use max(X,Y,Z), NOT height-only — Phase 6.2 REVERSAL](patterns/building-maxdim-normalization.md) — Y-only normalization (size.y) inflates XZ for wide/squat buildings (Chum Bucket 2× over-wide, Patrick's Rock dome balloon). max(X,Y,Z) normalization (`targetMaxDim`) gives consistent visual cube size. Supersedes the prior "use size.y" rule. Applied Phase 6.2 (2026-05-18). See patterns/building-maxdim-normalization.md.
- [Decorative child meshes inflate XZ bbox and trigger MAX_FOOTPRINT cap](gotchas/decorative-meshes-inflate-xz-bbox.md) — Flowers/Path groups in pineapple-house.glb bloat XZ to 1852wu, triggering footprint cap and shrinking height to 432; fix: stripDecorativeMeshes() before stripGroundPlanes()
- [Building GLB pivot offset — geometry far from scene origin (XZ rotation-aware + Y grounding fix)](gotchas/building-glb-pivot-offset-far-from-scene-origin.md) — inner group carries ALL three offsets: -pivotOffsetX/Z (XZ centering, rotation-safe) and -pivotOffsetY (Y grounding: bbox.min.y*scale); cures floating (min.y>0), underground (min.y<0), and XZ drift at any rotY
- [stripGroundPlanes full-bounds must use non-skinned bbox](gotchas/strip-ground-planes-skinned-mesh-inflation.md) — setFromObject inflates fullHeight for scenes with rigged nodes, causing real structural geometry to be wrongly stripped as a "ground plane"
- [NPC bbox normalization must use bbox.max.y not size.y](gotchas/npc-scale-bbox-max-y-not-size-y.md) — size.y inflates h when geometry extends below pivot; use max.y for above-pivot visual height; also tighten CLAMP_MAX to CH/0.5; make scaleOverride unconditional
- [GLB pivot not at feet — characters render underground](gotchas/pivot-not-at-feet-y-offset.md) — humanoid/anime GLBs pivot at waist; at NPC_SCALE=50 the offset is 25-75 world units underground; fix: measure localMinY * scale and subtract from position.y each frame
- [AVATAR_SCALE must be calibrated against actual GLB bbox — never assume from comments](gotchas/avatar-scale-vs-glb-native-height.md) — lobster.glb native height=1.12; AVATAR_SCALE=20 gave 22.4 wu (2× smaller than NPC 45 wu); verify with GLTF accessor bounds before setting flat scale constants

- [Room-scale vs world-scale geometry in a scaled interior scene](patterns/interior-room-scale-vs-world-scale-geometry.md) — Y-dimension of props must be calibrated against avatar height (270wu), NOT room max-dim (2000wu); XZ footprint can follow room scale. Also: `computeVRMAvatarFit` gains optional 3rd `targetHeightOverride` param for scene-specific overrides without polluting SPECIES map. (2026-05-18)
- [Interior chase camera AABB clamp — prevents wall-clip black void](patterns/interior-camera-clamp.md) — `clampCameraToRoom(pos, RoomBounds)` in `room-camera.ts` mutates desired cam position in-place before lerp; no allocations; AABB sufficient for rectangular rooms; raycast path available for curved walls. (2026-05-19)

## Building Scale Patterns
- [Building scale max(X,Y,Z) normalization — targetMaxDim](patterns/building-maxdim-normalization.md) — Normalize to max(X,Y,Z) not Y-only; prevents wide/squat GLBs from ballooning in XZ. All buildings now use `targetMaxDim` in `BUILDING_MODELS`. MAX_FOOTPRINT=1800wu hard cap still applies. (Phase 6.2, 2026-05-18)
- [childScaleOverrides + bodyAnchorChild — differential scale + body-centered placement](patterns/child-scale-overrides.md) — When a GLB bundles a building body + pathway/sign, the pathway/sign can dominate the max-dim bbox and compress the body. Solution: `childScaleOverrides` boosts the body node's scale; `bodyAnchorChild` shifts pivotOffsetX/Z so the body's bbox center lands at the ring slot (not the full-GLB center pulled toward the pathway). NEVER combine bodyAnchorChild with pivotZBias — double-offsets. Apply AFTER strip passes, BEFORE merge. (2026-05-18)

## WebGPU
- [WebGPU renderer setup with fallback](webgpu/renderer-setup-fallback.md) — detection, init, WebGL fallback pattern
- [NEVER await import('three/webgpu') inside renderer factory — dual-instance crash](gotchas/webgpurenderer-catch-path-plain-webglrenderer-dual-instance.md) — Dynamic import creates a second webpack chunk with a separate module instance; IndexNode/NodeShaderStage from instance B are different objects from materials registered via extend(THREE) (instance A). Result: IndexNode.VERTEX=undefined during shader compilation → SES_UNCAUGHT_EXCEPTION on Chrome/Brave without navigator.gpu, first-time visitors only. Fix: use THREE.WebGPURenderer from the static import at the top. Also covers: catch block using WebGLRenderer from plain 'three' (same root issue). (2026-05-21, commit 7350cfb)
- [iOS Safari black scene — navigator.gpu undefined crashes WebGPUBackend.init()](gotchas/ios-safari-webgpu-navigator-gpu-undefined.md) — `navigator.gpu.requestAdapter()` throws TypeError on iOS (navigator.gpu is undefined). While getFallback() catches it, iOS WebKit leaves canvas in bad state → pure black 3D viewport. Fix: detect iOS + WEBGPU_ABSENT, pass `forceWebGL:true` to WebGPURenderer. TSL node materials fully work on WebGL2 backend. (2026-05-20)

## iOS / Mobile
- [Conditional early-return in page component resets loader to 0% on iOS](gotchas/react-conditional-return-remounts-loader.md) — `if (isLoading) return <Loader />` before main layout mounts a fresh Loader instance when auth resolves → loader resets from mid-progress to 0% ("loaded twice"). Fix: remove early-return; let Canvas mount unconditionally; gate individual UI on hasAvatar. (2026-05-20)

## Performance
- [Loosen-while-loading pattern — load-phase throttles must switch on __W3D_READY](performance/loosen-while-loading.md) — rIC/rAF texture upload and VRM parse concurrency use fast constants while `!__W3D_READY` (overlay up, no frame budget to protect), gentle constants after `__W3D_READY`. Second `compileAsync` fires via `registerBulkVRMIdleCallback` once VRM parse queue drains. Pattern confirmed: 3 serialized load costs, all loosened under spinner, restored post-reveal. (perf round-3, 2026-06-15)
- [registerBulkVRMIdleCallback — VRM parse queue drain hook for compileAsync](performance/vrm-bulk-idle-callback.md) — exported from `vrm-loader.ts`; fires exactly once when `vrmParseActive===0 && queue===empty`; early-register path uses microtask; registered from `PreCompilePipelines` to kick second `compileAsync` so skinned-MeshStandard pipeline variants compile under the spinner, not lazily at reveal. (perf round-3, 2026-06-15)
- [Fog density directly controls fragment count on Iris Xe](performance/fog-density-iris-xe-regression.md) — fog far > camera.far wastes GPU; pushing 1200/6400→1800/9000 dropped FPS 90→50 on Iris Xe; always keep fog far ≤ camera.far
- [Draw call reduction techniques](performance/draw-call-batching.md) — instancing, merging, LOD, measurement targets
- [Deferred useGLTF.preload() via rAF](performance/deferred-preloads-pattern.md) — move non-critical preloads post-paint; rAF inside useEffect; Suspense fallback={null} absorbs cache-miss throws
- [compileAsync eliminates post-mount pipeline hitch](performance/compile-async-precompile.md) — call AFTER first R3F commit (not in onCreated); rAF-gate inside child component; no-op guard for WebGL
- [Draco compression results + drei auto-decode](performance/draco-compression-results.md) — texture-heavy GLBs barely compress; geometry-heavy get 5-20%; drei DRACOLoader is zero-config

## Patterns
- [BatchedMesh for static multi-material GLBs — intra-building batching only](patterns/batched-mesh-static-buildings.md) — BatchedMesh can only batch geometries sharing ONE material instance. Cross-building batching impossible (unique textures per building). Within a building: group meshes by material.uuid, one BatchedMesh per group. Bake full world transform (position+rotation+pivotOffset+scale) into vertex positions via applyMatrix4; set each instance matrix to identity. Requires exact pre-allocation (maxVerts+maxIdx upfront, r182 Constraint #1). Non-indexed geo: synthesise Uint32Array index budget = pos.count. BatchedMesh.dispose() cleans up internal geometry+textures. Anchor label overlay with lightweight Object3D added imperatively to threeScene (not R3F tree). (2026-05-09)
- [Variable cliff band width via mulberry32 hash — German River realism](patterns/variable-cliff-width-german-river.md) — replace constant LATERAL_MAX with per-section mulberry32 hash [180,600]wu for organic non-uniform silhouette; bounds-check at coral hw=880 confirms safe (2026-04-29)
- [Spline-tiled CC0 boulder cliffs — geometry extraction + mergeGeometries](patterns/spline-tiled-cc0-cliffs.md) — extract GLB primitives, bake instance transforms, mergeGeometries per bank → 2 draw calls; includes Poly Pizza CDN scraping + FBX2glTF scale-bake pipeline (2026-04-29); 3 critical bugs fixed 2026-04-29: clone-before-mutate, useMemo scenes array, dispose-on-unmount
- [useGLTF cached scene must be cloned before mutating transforms — Fix for rocky-cliffs.tsx](gotchas/useGLTF-scene-mutation-clone-first.md) — extractAndTransformGeos() called position.set/rotation.set/scale.set directly on the THREE.Object3D from useGLTF, mutating the shared cache. Fix: srcGroup.clone(true) → mutate clone → discard; never touch srcGroup directly. (2026-04-29)
- [Instanced boulder cluster cliff — InstancedMesh per GLB variant along spline](patterns/instanced-boulder-cluster-cliff.md) — extract mesh from GLTF scene into IM; two-layer stacking (ground + upper) for chaotic rim; DO NOT dispose geo/mat on unmount (owned by GLB cache); 3 draw calls for ~400 instances (2026-04-29)
- [Narrow long corridor ground strip for racing-game perspective](patterns/corridor-narrow-ground-strip.md) — replace wide square with 4000×24000 corridor; X-segs scale proportionally (96→32); displacement mask + bank shadow smoothstep thresholds must track corridor half-width; 24,576 fewer tris; river dominates visually (2026-04-29)
- [Spline-following ground ribbon — swept BufferGeometry along Catmull-Rom spline](patterns/spline-following-ground-ribbon.md) — buildGroundRibbonGeo(side, samples, widthSegs): inner edge = hw + GROUND_INNER_OFFSET; mirrored winding for right side (+Y normal); samples=128 widthSegs=64 → 32k tris; no position prop on mesh (verts in world XZ); old displacementMask must be removed or set 1.0 (2026-04-29)
- [drei shaderMaterial() factory — modern R3F terrain pattern](patterns/drei-shadermaterial-terrain.md) — shaderMaterial({uTime:0}, vert, frag, onInit) + extend({TerrainMaterial}); onInit sets fog/side/transparent; ThreeElements['shaderMaterial'] base type (NOT JSX.IntrinsicElements — compile error); ref typed as InstanceType<typeof TerrainMaterial>; mat.uTime = val proxies to uniforms (2026-04-29)
- [Animated spline-following karts (GLB clone per-frame)](patterns/animated-spline-kart.md) — clone-once + useFrame: advance t, sample centerlineAt/tangentAt/normalAt, set position+rotation, updateMatrix; banking via finite-diff cross product; combine clone+attach in ONE useEffect; no shaderMaterial needed for tinted-GLB karts (2026-04-29)
- [Spline-following ribbon terrain stack (ground + sand + water)](patterns/spline-ribbon-terrain-stack.md) — replace rectangle water plane with 3-layer stack swept along spline; xJitter must exceed halfWidth to keep props off water; MeshLambertMaterial flatShading opaque; HEMI_GROUND_COLOR matches ground plane (2026-04-29)
- [Rocky cliff ribbon — stepped cross-section swept along spline](patterns/rocky-cliff-spline-ribbon.md) — 5-vert profile per side, 79 cross-section pairs, 1264 tris, 1 draw call (mergeGeometries both banks); vertex colours: slow sine-wave striations + hash jitter + lower-cliff darkening; winding order differs left vs right bank for inner-face visibility; WATER_Y=-40 canyon state wired in iter-5 (2026-04-29)
- [WATER_Y local constant isolation — racing-karts.tsx owns its own copy](gotchas/racing-karts-local-water-y-mismatch.md) — racing-karts.tsx has its OWN local `const WATER_Y` (not imported from river-scene.tsx); every river-scene cascade MUST also update racing-karts.tsx. iter-6: both now at -200. (2026-04-29, updated iter-6)
- [Canyon wall backfill ribbon — vertical wall swept along spline to seal boulder gaps](patterns/canyon-wall-backfill-ribbon.md) — DoubleSide MeshStandardMaterial + 4-vert cross-section (inner/outer × bottom/top); 128 samples, 1024 tris both sides; module-scope geo built once; renderOrder=0 behind boulders; computeVertexNormals() for flat shading (iter-10, 2026-04-29)
- [Catmull-Rom spline for server-sim race corridors](patterns/catmullrom-server-sim-spline.md) — shared spline struct (control points + arclength LUT) between server wall-collision math and Three.js visual track; closestPointOnSpline via Newton; replaces ellipse helpers
- [Centripetal Catmull-Rom spline — Barry-Goldman + arclength LUT](patterns/centripetal-catmullrom-spline.md) — full production implementation; CRITICAL: dC/dtK * kRange for arc integration (not segment-local span); phantom endpoint reflection formula; Newton residual uses unnormalized tangent; 46.7ms/1000 calls; all 34 tests pass (2026-04-28)
- [Transparent ghost kart — shared module-scope geo/mat + fade loop](patterns/transparent-ghost-kart-pattern.md) — BoxGeometry at module scope, depthWrite false, lap-relative looping via elapsedMs%pathDuration, fade from loop position, O(1) sequential scan with wrap-around guard, localStorage gate
- [Multiplayer position interpolation (15Hz → 60fps)](patterns/multiplayer-position-interpolation.md) — Render-behind at now-100ms; 4-slot history ring; object-identity snapshot detection; shortest-angle rotation lerp; NaN guard for zero-velocity facing; zero per-frame allocations.
- [Spectator camera modes — follow/free/action](patterns/spectator-camera-modes.md) — Dual-canvas strategy (ortho for active play, PerspectiveCamera canvas for spectators); OrbitControls created/disposed on mode change; module-scope scratch vectors; action mode retargets to arena-center entity every 3s
- [Route-isolated activity scene pattern](patterns/route-isolated-activity-scene.md) — Canvas key={roomId} isolation, perspective chase cam (rebuilt 2026-04-24, ortho removed), module-scope burst pool (6 slots×12pts), gravity-drop elimination, DOM screen flash, fog calibration for perspective cam, Html label dot-product cull, PreCompilePipelines last-child rule
- [Perspective chase camera + DOM screen-edge flash](patterns/perspective-chase-camera-screen-flash.md) — Velocity-derived yaw (dead-reckons when stopped), exp-decay lerp arm, shakeRef mutable ref (not state) shared between HitEventProcessor and ChaseCameraController, position:absolute DOM overlay for screen-edge red flash (zero draw calls, no alpha-sort), fog tuning formula for perspective cam
- [Reef Race 3D scene](patterns/reef-race-scene.md) — Per-client chase-cam, TubeGeometry track (radialSegs=4), merged checkpoint gates (2 draw calls), InstancedMesh zero-scale hiding, ghost kart 10Hz lerp path, boost BufferGeometry ring-buffer trail, additive store extension pattern
- [Reef Race Phase 2 static visuals](patterns/reef-race-phase2-static-visuals.md) — server RoomMeta zones → client meshes; module-scope geo+mat; primitive store subscription; fallback local builders; 8 draw calls for ribbons+hazards+apex markers
- [Reef Race RoomMeta one-shot participant metadata pattern](patterns/reef-race-participant-meta-snapshot.md) — stamp display-only per-avatar data (species/GLB) into RoomMeta on snapshot.init; re-inject on keyframe using stored map; emptyState Pick union MUST include the field or reset() silently skips wipe (2026-04-29)
- [FBXLoader parallel multi-file clip extraction](patterns/fbx-loader-parallel-clips.md) — Promise.all loads character + N animation FBXs; rename clips from "mixamo.com"; attach to template.animations; SkeletonUtils.clone; AnimationMixer; React 19 `use()` for suspension; Mixamo bone-name resolution automatic
- [Jump system: module-scoped state + JumpTicker](patterns/jump-system-module-scoped-state.md) — charge-and-release (2026-04-21): hold SPACE on ground → charge bar fills → release proportional launch (tap=33wu, full charge=1531wu); 5 phases: grounded/charging/quick/launch/sinking; no apex-freeze, no peak clamp; 'charging' is not airborne; idle rotation freeze preserves last facing
- [WorldLabelsOverlay — single DOM overlay replaces 30+ drei Html portals](patterns/world-labels-overlay.md) — Module-scope registry + single rAF projection pass; zero per-frame allocations; NDC z>1 behind-camera cull; consumers call useWorldLabel()+WorldLabel portal; mount WorldLabelsOverlayMount early in SceneContents (2026-05-09)
- [WorldLabelsOverlay — distance fade + building-occluder raycast](patterns/world-label-distance-fade-occlusion.md) — LabelEntry gains fadeNear/fadeFar/fadeBaseOpacity/occlude; 10 Hz staggered raycast against userData.isOccluder building meshes; module-scope zero-alloc; NPC 800→3000wu 0.65 base, buildings 2000→5000wu 0.40 base; visual: no pill, uppercase/italic wordmarks + glow shadow (2026-05-18)
- [drei Html for NPC labels and speech bubbles](patterns/drei-html-npc-labels-bubbles.md) — DOM overlay, safe on Iris Xe; use for all NPC name labels and chat bubbles (never use drei Text/Billboard) — SUPERSEDED by WorldLabelsOverlay for new label sites
- [NPC label occlusion via cached occluder-mesh raycast](patterns/npc-label-occlusion-raycast.md) — userData.isOccluder tag + lazy mesh cache + 10Hz staggered raycast; hides labels behind buildings/NPCs; <0.3ms/frame on Iris Xe (2026-04-25)
- [Circular building ring — slot math, rotY formula, tilemap constraint](patterns/building-ring-circular-slot-math.md) — Phase 6.1: 12-slot circle at R=100 tiles on 240×240 grid; θ=-π/2+slot*π/6; rotY=atan2(120-cx,120-cy); R≤113 max on 240-grid; posX/Y=zone_x*TILE_SIZE. rotY values IDENTICAL to R=72 (angle-only). (2026-05-18)
- [Building targetHeight override — per-building scale without bypassing bbox pipeline](patterns/building-target-height-override.md) — Phase 6.1: ALL 12 buildings have explicit targetHeight overrides (900–1500wu); 800wu default is fallback only. MAX_FOOTPRINT 1000→1500 (wide buildings capped at wrong value for R=100 ring). pivotZBias: extra inner-group Z offset for step/foreground displaced bbox centers. Never use scaleOverride for factor-based resize. (updated 2026-05-18)
- [Box3 auto-fit with matrixAutoUpdate lock — bake scale+offset before locking](patterns/box3-auto-fit-with-matrixautoupdate-lock.md) — When auto-fitting a GLB (scale+center offset) then locking matrixAutoUpdate=false, bake BOTH transforms into cloned root's .scale and .position BEFORE updateMatrixWorld(). DO NOT put offset on outer <group position={...}> — R3F prop reconciliation races with the useEffect lock. (2026-05-18)
- [ClawVille world proportion constants](patterns/clawville-world-proportions.md) — canonical building ring coords, heights, camera, seaweed exclusion radii — updated 2026-04-15 for 160x160 square map (5120x5120)
- [NPC possession WASD controller](patterns/npc-possession-controller.md) — WASD drives possessed NPC, wander skips it via lazy-require circular-dep workaround
- [Arrow key orbital camera rotation](patterns/orbit-controls-arrow-key-rotation.md) — spherical coords to rotate OrbitControls view; separate WASD pan from arrow key orbit
- [FPS-style follow camera with OrbitControls](patterns/fps-follow-camera-orbit-controls.md) — lerp target + enforce radial distance; controlMode routing; scratch vectors to avoid GC
- [TSL MeshStandardNodeMaterial sand terrain](patterns/tsl-node-material-sand.md) — colorNode/roughnessNode/normalNode + vertexColor() + positionLocal height-tint pattern
- [Low-poly stylized river atmosphere for R3F (Iris Xe safe)](patterns/reef-race-river-atmosphere.md) — SkyDome vertexColors BackSide sphere, MeshLambertMaterial flatShading water with vertex wave, ScenerySpawner per-GLB Suspense(fallback=null). FOG_COLOR '#a8d8ff'. Hard pivot from THREE.Water class (not plain ShaderMaterial — see below) to this pattern. (2026-04-29)
- [Procedural cartoon water shader — simplex noise foam stripes + UV scroll](patterns/procedural-water-shader-foam-stripes.md) — Plain ShaderMaterial on plain Mesh IS Iris Xe safe (crash gotcha is InstancedMesh+ShaderMaterial only). Multi-octave sin wave vertex displacement + inline 2D simplex noise foam stripes + UV-scrolled flow. Module-scope mat + single useFrame uTime update. (2026-04-29)
- [Arcade cel-shaded water — hard-step stripes + lateral wave-lines](patterns/arcade-cel-water.md) — Pure-cyan flat base, step() foam (no smoothstep), lateral fract() wave-lines (Wave Race 64 style), pulsing edge foam, value hash (not simplex) for intentional aliasing. Reads as water at all altitudes. Competing against water-surf.tsx gradient approach. (2026-04-29)
- [Surf-game-quality water shader — depth + refraction-feel + foam + sun glint](patterns/surf-game-water-shader.md) — UV.x edge-to-center depth shift (min(uv.x,1-uv.x)); fake refraction via worldZ phase sin offset; dual-octave simplex at scale 12/8 (not 56+ which aliases to grey from altitude); smoothstep(0.60,0.65) foam crests; pulsing edge foam at 628wu period; fake Phong sun glint. Build verified 2026-04-29.
- [Organic foam cluster technique — softField × clusterMod](patterns/organic-foam-cluster-technique.md) — Replace PS2 harsh linear stripes: smoothstep(0.40,0.78) [5.4× softer] × snoise(vUv*24)*0.5+0.5 remapped to [0.5,1.0] via mix(). Never fully zeroes foam. 3rd snoise call, safe on Iris Xe. Shipped water-surf.tsx 2026-04-29.
- [drei shaderMaterial() factory — modern R3F pattern for custom materials](patterns/drei-shadermaterial-water.md) — shaderMaterial(defaults,vert,frag) + extend({WaterMaterial}) at module scope; uniform defaults become JSX props + typed setters; matRef.current.uTime = n (no .uniforms.xxx.value); TS: InstanceType<typeof WaterMaterial>; key={WaterMaterial.key} for HMR. Build verified TypeScript-clean 2026-04-29.
- [TSL underwater atmosphere](patterns/tsl-underwater-atmosphere.md) — caustic plane (MeshBasicNodeMaterial + AdditiveBlending + uv()), depth backdrop (gradient via uv().y mix), dust particles (PointsNodeMaterial + positionNode fract drift)
- [TSL volumetric light rays](patterns/tsl-volumetric-light-rays.md) — open-ended CylinderGeometry cones + AdditiveBlending + sin(time) pulsing opacityNode, 7 draw calls, zero CPU
- [Multi-variant merged seaweed with per-blade TSL amplitude](patterns/merged-seaweed-multivariant.md) — 3 blade shapes, cluster distribution, aAmplitude attribute drives per-variant sway in TSL, two-wave oceanic motion

- [Procedural skirt parented to hip bone](patterns/procedural-skirt-bone-parenting.md) — CylinderGeometry cone + bone.add(); follows rig without vertex weights; open=true for fabric look; DoubleSide; module-scope geo/mat (don't dispose)
- [Mixamo → VRM retarget pipeline](patterns/vrm-mixamo-retarget.md) — bone name map + track rewrite at load time, VRMCharacterAnimator crossfade, VRM faces -Z (opposite of lobster), no tinting, feet at origin
- [VRM wandering NPC pattern](patterns/vrm-wandering-npc.md) — VRMNpcMesh in arena-npcs, VRM_NPC_SCALE=28 (45wu target), VRM_DIR_ROTATION, no pivot offset, no tint, single-instance-per-path cache constraint, preload at module scope
- [VRM NPC grounding — no +2 baseline, no pivotOffsetY](patterns/vrm-npc-grounding-no-baseline-offset.md) — VRM Y = terrainY + bob + jumpY (no +2, no pivotOffset); GLB needs both; verified vs player-avatar.tsx VRM branch. Jump block ported from GLBNpcMesh 2026-04-25.
- [Universal procedural character animation](patterns/universal-character-animation.md) — spatial mesh analysis + per-type motion profiles, softLerp hot path, routes 9 new GLBs while preserving LobsterAnimator
- [Companion NPC pattern](patterns/companion-npc-pattern.md) — passive NPC beside primary; same NpcMesh with showLabel=false; seed+17 for staggered raycasts; rotYOffset field on NpcModelConfig corrects +X-forward GLBs (Gary=-π/2)
- [KTX2Loader wiring for drei useGLTF with WebGPU support](patterns/ktx2-loader-wiring.md) — singleton + Canvas component + extendLoader; basis WASM from three/examples/jsm/libs/basis; three/addons KTX2Loader required (not three-stdlib)

- [Sea-creature rigged GLB animation pipeline](patterns/sea-creature-glb-pipeline.md) — manifest-gated async factory for non-humanoid GLBs; null=static-mesh fallback; loop/one-shot states; hit auto-reverts; no retargeting; module-scope cache + MeshoptDecoder
- [Static-mesh transform-only swim animator](patterns/static-mesh-transform-swim.md) — rotation.x/z + position.y oscillation on whole mesh group for 0-bone GLBs; hasBones probe cached per avatarId; speed envelope; NEVER touches rotation.y (server owns it); zero allocations; 16 callsites/frame safe on Iris Xe
- [Standalone R3F avatar preview page](patterns/standalone-avatar-preview-page.md) — /preview/avatar?mode=X; useSearchParams+router.push for URL-driven mode; module-scope GLB cache + MeshoptDecoder; skeletonClone+frustumCulled=false on every load; useCallback stable refs prevent AvatarScene remount on parent state change; sketchfab 404 surfaced as DOM overlay not canvas text (2026-04-26)
- [Cosmetic bone anchor — attach GLB accessory to avatar head bone](patterns/cosmetic-bone-anchor.md) — findBone() with VRM/Mixamo/glTF name candidates; wrapper Group for offset/scale/rotation; frustumCulled=false mandatory; module-scope GLB_CACHE with clone(true); dispose pattern (2026-04-28)
- [Aura shader — GLSL fresnel sphere for avatar glow](patterns/aura-shader.md) — ShaderMaterial NOT TSL (WebGLRenderer world canvas); fresnel + sin pulse; module-scope SphereGeometry singleton; compileAsync after attach; uTime via userData.cosmeticUniforms (2026-04-28)
- [Palette texture swap — replace avatar albedo map at runtime](patterns/palette-texture-swap.md) — TextureLoader + colorSpace=SRGBColorSpace; save/restore originalMaps on dispose; skip isMToonMaterial; UV-region compositing deferred (2026-04-28)

- [WebP texture compression for GLBs](performance/webp-texture-compression.md) — gltf-transform+sharp converts PNG→WebP in GLBs (83% P1 / 66% P2 wire savings); includes long-task regression warning
- [KTX2 UASTC vs WebP wire size](performance/ktx2-uastc-vs-webp-sizing.md) — UASTC is 4-5x LARGER than WebP on wire for cartoon GLBs; ETC1S is wire-comparable but lower quality; UASTC only wins on GPU memory
- [Staggered GPU texture uploads via initTexture()](performance/staggered-texture-upload.md) — renderer.initTexture(tex) per-frame (2/rAF) spreads 400ms+ WebP decode+upload long task; works on both WebGL r170 and WebGPU r182

- [2026-04-21 perf sweep — static mesh freezing + scratch patterns](performance/perf-sweep-2026-04-21.md) — matrixAutoUpdate=false on all static scene objects; module-scope scratch vectors/matrices in hot loops; intersectObject(mesh,false) over scene traversal; distanceFactor removal from <Html>; narrowed Zustand subscriptions; gl.setPixelRatio() override removal. Est. ~4-6ms/frame saved → 60+ FPS target.
- [Zustand re-render storm fixes (B5+B6+B7)](performance/zustand-re-render-storm-fixes.md) — useShallow on array selectors; petPositionRef module-scope + 10Hz throttled reactive write eliminates 60Hz Minimap rebuild; NPC object identity preservation in updateFromSnapshot restores React.memo bailout. (2026-04-24)
- [Idle animation throttle to 20Hz](performance/idle-animation-throttle.md) — `(frame + seed) % 3 === 0` gate on slow procedural animations (≤1.3 rad/s); walk stays 60Hz; stagger by seed prevents batch spikes; saves ~40% trig ops on 12-30 NPC instances.
- [VRM spring-bone physics throttle — 30Hz for idle NPCs](performance/vrm-spring-bone-throttle.md) — split VRMCharacterAnimator into updateMixerOnly (60Hz) + updateSpringOnly (30Hz idle); accumulate delta between spring ticks; walking stays full 60Hz; 50-100 spring joint ops/frame → halved for idle VRMs.
- [VRM draw-call reductions — MToon outline off, removeUnnecessaryJoints, lookAt/expressionManager disable, skeleton.update batching](performance/vrm-draw-call-reductions.md) — 4 load/init-time techniques; halves draw calls per VRM mesh; cuts bone CPU 20-40%; eliminates 3× redundant skeleton.update calls per VRM. Do NOT apply to SpongeBob GLBs — they are not VRMs.
- [meshopt + WebP asset pipeline (C6)](performance/meshopt-webp-asset-pipeline.md) — `bun run assets:optimize` compresses all GLBs/VRMs: 45.9 MB → 14.5 MB (-68%). VRM extension reinject required; temp path must end in .glb; prune skipped for VRMs.

- [Phong glint reflects away from +Z chase cam when sun has +Z component](gotchas/phong-glint-reflects-away-from-chase-cam.md) — dot(reflect(-sunDir,(0,1,0)), viewDir) is negative when sun.z>0 and cam faces +Z → zero glint; scalar bump only helps on curve segments; real fix is sun.z<0 (2026-04-29)
- [VRM activity rider — Suspense-wrapped per-instance VRM on activity prop](patterns/vrm-activity-rider-pattern.md) — useVRMInstance + disposeVRMInstance; Rules of Hooks sentinel path; wipeout teleport heuristic (500wu threshold); victory finishedRef guard; VRM_RIDER_LOCAL_SCALE=5.6; frustumCulled=false mandatory (2026-04-29)
- [VRMCharacterAnimator.setSurfaceClip() — activity-scoped resting animation](patterns/vrm-character-animator-surface-clip.md) — private surfaceClip field defaults to 'idle'; call setSurfaceClip('surf_idle') after init() so wipeout/victory one-shots crossfade back to surf_idle; dispose() resets BEFORE actions={} (2026-04-29)

- [Spline-ribbon world-XZ noise + large amplitude = sawtooth from altitude](gotchas/ground-ribbon-world-xz-noise-sawtooth.md) — outer ribbon edge covers 2.4× more world-XZ per t-step than inner on curves → non-uniform noise + ±50wu amplitude → zigzag shark teeth from 23770wu altitude; fix: UV-space noise + ±12wu cap
- [Ground ribbon winding order must differ by side — naive "mirror" is backwards](gotchas/ground-ribbon-winding-order-per-side.md) — side=+1 vertices extend in -X (n.x<0): tri (a,b,c) gives +Y normal (v1=(-Δx,0,0)×v2=(0,0,+Δz)→+Y). side=-1 vertices extend in +X: tri (a,c,b) gives +Y. The two branches look like a swap vs. naive mirror — that's correct. Previous code had them backwards → both ribbons backface-culled → zigzag. Commit f775b958. (2026-04-29)
- [Scenery xJitter must exceed cliff outer envelope (873wu) — props inside cliff zone otherwise](gotchas/scenery-xjitter-inside-cliff-zone.md) — spawnPos dist=hw+xJitter; cliff outer edge=hw+873; xJitter<873 buries props in rock. Also: multiple SpawnerDef entries per path need unique `key` field (not `path`) for React. (2026-04-29)

## Gotchas (more)
- [VRM half-rate early-return gate kills mixer at mid-distance](gotchas/vrm-half-rate-gate-kills-mixer.md) — early-return above animator calls throttles the mixer too, not just spring bones; always gate spring-bone only, keep mixer call unconditional at 60Hz.
- [skeleton.update restore must use Map, not index-aligned Array](gotchas/skeleton-update-array-traversal-order.md) — index-aligned Array misaligns if scene graph mutates between construction and disposal; use Map<THREE.Skeleton, fn> + Map.forEach in dispose; also `for...of map` iterates [k,v] pairs — use `.values()` to iterate functions.
- [useLayoutEffect not useEffect for VRM property mutations — closes 1-frame R3F race](gotchas/uselayouteffect-vs-useeffect-vrm-frame-race.md) — R3F useFrame runs in rAF before useEffect fires; use useLayoutEffect to null vrm.lookAt/expressionManager before first frame paints.

## Solutions
- [mergeGeometries dispose-after-merge is safe — data is copied](solutions/merge-geometries-dispose-order-safe.md) — mergeAttributes() uses TypedArray.set() to copy; dispose only removes GPU buffers; merged geo is independent
- [Lobster model facing + correct atan2 formula + screen-relative movement](solutions/camera-relative-joystick-input.md) — lobster.glb faces +Z (THIRD REWRITE 2026-04-16 late PM — clean side-view); use atan2(vx,vy); DIR_ROTATION down=0,up=PI,right=PI/2,left=-PI/2,idle=0; movement SCREEN-RELATIVE
- [Avatar scale-down pass 2026-04-16](solutions/avatar-scale-down-2026-04-16.md) — AVATAR_SCALE 55→33, TARGET_NPC_HEIGHT 120→75, CHARACTER_HEIGHT 140→90, SPEED 200→320; HARD_MAX and scaleOverride must update proportionally when target heights change
- [Module-scope scan ref → useRef for per-instance isolation](solutions/module-scope-scan-ref-per-instance.md) — any mutable cursor/accumulator for one component lifecycle must live in useRef not module scope; pass scanRef.current into the helper function; identity check on path ref still works (2026-04-26)

- [Full movement audit 2026-04-14 — screen-relative verified correct](patterns/full-movement-audit-2026-04-13.md) — screen-relative movement confirmed; camera-relative revert documented; -Z model formulas verified
- [merged-seaweed already uses MeshBasicNodeMaterial — no Lambert swap needed](gotchas/merged-seaweed-already-meshbasic.md) — merged-seaweed.tsx uses MeshBasicNodeMaterial (zero lighting cost, cheaper than Lambert); verified 2026-04-24; B8 "Lambert swap" was already superseded.
