# Deploy Status — cross-session staging/prod deploy ledger

> **Single source of truth for "what is on staging/prod right now, and what broke last time."**
> Updated SAME-DIFF on every push to `staging` (and on every `staging → master` promotion).
> See the `deploy-status.md` rule in `CLAUDE.md` (Documentation Update Policy + PUSH FLOW).
>
> **Authority to set CURRENT STATE = whoever pushed last** (tiebreak: `git log -1 origin/staging`).
> A session that did NOT push last may ADD a DEPLOY LOG entry but must not overwrite CURRENT STATE.
>
> Be HONEST about what broke and why — the whole point is that the *next* session (which may be a
> different person/agent working a different branch) inherits real context, not a happy-path summary.

---

## CURRENT STAGING / PROD STATE

- **2026-07-27 (session notDev, P1c) — CURRENT: `staging` = merge `f9786879`.** **P1c presence continuity + navigation hardening + the 60-loop leak-soak gate.** Your body now STAYS in the shared room while you're at the Cove — other players see it idle at your last town position with an "at the Cove" tag (layout-owned world stream; 10 s frozen-pose heartbeat; cold `/cove` still joins nothing; watch-heartbeat gated so Cove play never signals a paid banter audience; avatar heartbeat stays live so the bound agent body remains suppressed = Cove play counts as continued human control). First-crossing navigation is now durable (buffered bridge, route-generation invalidation, phase-aware adopt/supersede; browser history bounded at 4 instead of growing 2/crossing to 122). The new soak gate caught + fixed FOUR real leaks (activation-remounted cove/world lights+labels+hotspots; history growth; incomplete off-frustum warmup); the residual ~0.45 MB/crossing was heap-snapshot-NAMED as three r185 WebGPU-internal bindGroups caching (follow-up tracked for the r186+ upgrade / eviction tier) and the gates recalibrated on that evidence (v4/v4.1 rulings in `docs/world-stage-p1c-brief.md`). **PROTOCOL_VERSION 39→40** (self-reported `at-cove` activity convention documented; Nori knowledge + hatcher spec reconciled). Built via /copus-max (Codex critiques R1+R2 → Codex implement → 2 Codex leak legs → Fable review of every diff + Fable-run final gates). Verified pre-push: build 0 · web tsc 0 · web+api tests green · synthetic WebGPU+WebGL 102/102 · routes 30/30 (join phases 0/1/0, cold-init) · **soak 60/60 ALL 28 assertions green (textures/geometries EXACT-flat from loop 30, renderer bytes −77 KB, heap slope 0.293 MB/loop vs 1.0 limit)** · dwell-game + dwell-cove drift ≤0.002 MB/s. **PARITY:** human path = `(world)` layout route policy; agent path = authenticated `/api/world/position` activity on the same wire; presence binds to the caller's session/avatar; no economy change. **SCHEMA:** `synced` — no migration. PENDING on staging: mock-Hatcher harness + onboarding smoke + hosted runtime probe (skill-protocol touched) + two-account presence drive + `/arena` smoke → then founder eyes (P1b fades + P1c presence + ONE demo slots spin) before staging→master.
- **2026-07-26 (session copusMax, autonomy walk fix) — PRIOR: `staging` = `49aeb93b`.** **Autonomous/house agents no longer crawl to destinations (was ~10 min to the cove).** A directed trip (an agent told to go somewhere) is now kept alive through wall grazes instead of thrown away: the server slides the body along the wall (NO pathfinding in the 200 ms sim tick — that A* was measured at 165-258 ms/call and would stall the shared event loop), and the 30 s autonomy driver owns re-routing (a stuck trip re-routes to the same goal without a fresh LLM decide; the give-up timer scales to how far it actually has to walk — 2 min floor / 3 min ceiling, 5 min hard episode cap; the walking phase now runs even if the cognition runtime is down). Directed routes are also shielded from ambient NPC planning/conversation so a connected-agent trip can't be hijacked mid-walk. Built via /copus-max (Opus-5-max spec+review, Codex gpt-5.6-sol implement; v1 was REJECTED for in-sim A*, v2 moves all A* to the driver). Verified pre-push: apps/api tsc 0; new directed-route 15/15 + walk-budget 17/17; full apps/api service suite 1742 tests, 0 fail. **PARITY:** human movement unchanged; hosted/house driver walks + connected-agent `enter_*`/authed building-moves share the same route persistence. **SCHEMA:** `synced` — no migration; no wire/PROTOCOL_VERSION change. AWAITING staging deploy verify (no-regression: ambient NPCs still wander) → founder go for staging→master → prod live Coralia walk proof. OPEN: the 8-20 s enter-cove activity FREEZE at trip start is a separate ~3-line fix, out of this scope (founder call).
- **2026-07-26 (repo-scrub session) — ⚠️ GIT HISTORY REWRITTEN REPO-WIDE (founder-directed purge).** ALL commit SHAs changed (5,872 commits filtered): `CLAUDE.md`, `AGENTS.md` (was a full public copy of CLAUDE.md), `.claude/`, and `*.itachi-bak` removed from every ref via `git filter-repo` + force-push. Tree content otherwise byte-identical — prod/staging containers unaffected (no rebuild needed; content identical). Post-rewrite `staging` = `f965657c` (was `ec9fca4f` — same tree minus purged files) + the `.gitignore` guard push on top; `master` = `5ff93532` (was `4c01598a`). **RULE for every checkout/worktree: any local branch created BEFORE 2026-07-26 must NOT be pushed as-is** — it carries pre-rewrite history and would resurrect the purged files; recreate from new `origin/<base>` and cherry-pick. SHAs quoted in older log entries below are pre-rewrite and now resolve only as orphaned GitHub objects.
- **2026-07-26 (session notDev, push 2) — CURRENT: `staging` = `ec9fca4f` pre-rewrite (P1b).** **The Cove joins the persistent stage — cove↔game crossings are now FADES with NO loading screen.** `/cove` (page only) moved into `app/(world)/`; the cove interior renders as a second slot in the ONE persistent canvas; `SeaLoadingScreen` never mounts on returns; cove deep-links load zero world assets; `/cove/history`+`/cove/verify`+poker stay outside the group; `CoveCanvas.tsx` deleted. useSceneFrame sweep across world+cove frame owners (full inventory in `docs/world-stage-p1b-notes.md`). Cove REST/economy byte-untouched; streams stay page-owned (presence moves in P1c). Codex authored (host-blocked twice by the Windows 0xC0000142 spawn failure — work recovered from a wip snapshot both times); Fable completed verification + review per Rule E3 with TWO blocking review fixes in-slice: scene-scoped label registry (world labels bled onto /cove — dual overlay hosts vs module singleton) and useSceneFrame render-priority support (preserves the -100 run-before-camera contract). Verified: build 0 · tsc 0 · ALL THREE probe lanes PASS on the final build (synthetic WebGPU+WebGL 102/102; real-route 30 game↔cove round trips: 1 canvas mount, 0 hidden violations, 0 listener delta, heap +12.5% under the 15% gate) · reviewer browser drive (cove renders in-stage, Back-to-World fade with loader never mounting, deep-link isolation). Known P1c items in notes (first-navigate cold-boot fallback, dual interior GLB download, heap watch, headless slots-spin gap → FOUNDER PLAYTEST should include one demo slots spin). **PARITY:** rendering architecture only. **SCHEMA:** `synced`. AWAITING staging deploy verify → founder eyes (cove↔game loop + one spin) before P1c.

- **2026-07-25 (session notDev) — CURRENT: `staging` = `cf393d4f`.** **Persistent world stage P0a+P0b+P1a** (plan `docs/persistent-world-canvas-plan-2026-07-24.md`, Codex-converged v2.1). WEB-ONLY rendering architecture: `/game` now renders through ONE persistent WebGPU/WebGL canvas mounted by the new `app/(world)/` route-group layout (`WorldStageRoot`); the game page keeps loader/HUD/streams byte-identical; `WorldScene` is a SHARED export from `World3DCanvas` (no fork — `/arena`+`/perf` still use the legacy default export); WorldWarmup reused via a stage adapter (single frameloop owner); legacy `__W3D_*` handshake + the `3f0dd4c3` cove-freeze watchdog retained (SeaLoadingScreen untouched). This slice is a PARITY CUTOVER — no visible behavior change intended; the fade-returns payoff arrives in P1b when `/cove` joins the group. Also carries the P0 stage module + dev proof route `/perf/stage?stage=1` (hidden, `?stage=1`-gated) + the 100-transition probe gate + byte VRAM ledger + consolidated renderer health policy (device-loss bounded recovery, session WebGL fallback). Codex implemented / Fable reviewed with one review fix (stage DPR corrected to live constants `[0.55,0.7]` — brief carried a stale doc value). Verified pre-push: build 0 · web tsc 0 · probe 102/102 PASS both backends · `/game` Cache-Control still no-store (force-dynamic guard survived the move) · real-browser drive: world+NPCs+HUD on stage ~58 FPS dev box, pointer layering correct, `/game`↔`/perf/stage` clean, `/arena` smoke green. **PARITY:** rendering architecture only — no economy/agent/protocol surface change (streams/presence move in P1c with the named worldPresencePolicy). **SCHEMA:** `synced` — no migration. **PROMOTED TO PROD 2026-07-26 (PR #250, master `4c01598a`)** — founder staging sign-off received; prod containers verified (both apps SOURCE_COMMIT `4c01598a`); prod `/game` browser-verified: full world renders (forced-WebGL lane screenshot-proven; default-lane white screenshot = the known WebGPU-swapchain headless-capture artifact, NOT a regression — ready flags + DOM label projection + identical code on staging confirm), Cache-Control still no-store. P1b (cove joins the stage, fade returns) in progress on `feat/world-stage-p0a`.
- **2026-07-24 (session SAPmcp, push 3) — PRIOR: `staging` = that push.** **Agent-pay 5¢ minimum + per-sender daily payment-count cap (founder-approved credit-burn control).** PayAI bills per settlement; prod ran ~1,725 settles/day of 1-5¢ payments (~51.7k/mo) with a FLAT 1-5¢ distribution — count, not dollars, drives the bill (a min size alone provably cannot reduce count in a circulating economy, so the cap is the lever). New env knobs: `AGENT_PAY_MIN_USD_CENTS` (default 5, floor 1 — new payments below refuse `amount_below_min` after the idempotency lookup, BEFORE any row/PayAI work; replays of pre-existing smaller rows unaffected) + `AGENT_PAY_DAILY_COUNT_CAP` (default 50, sender-side, counted inside the SAME admission aggregate SQL as the dollar caps with identical status/cap_exempt filters; 51st refuses existing `daily_cap_exceeded` code, `detail:'daily_count_cap'`). Ceiling: ~9 senders × 50 ≈ 450 settles/day ≈ 13.5k/mo. **PROTOCOL_VERSION 38→39**: manual documents both knobs; town-guide payment-limit sentence updated; orientation version refs synced (no payment section existed there — none added). Codex implemented / Opus reviewed + re-verified (tsc 0; agent-pay + resume + conformance + skill-protocol-onboarding + agent-paid-surface 85/85). **PARITY:** identical limits for human-directed and autonomous agent payments; connected + hosted agents learn the rules via the version-bumped manual. **SCHEMA:** `synced` — no migration. AWAITING staging deploy verify → promotion → prod live before/after settle-rate check.
- **2026-07-24 (session SAPmcp, push 2) — CURRENT: `staging` = this push.** **Recipient-ATA fail-fast** for outbound PayAI transfers: a read-only local simulation pinned the day's intermittent `verify:transaction_simulation_failed` (2,321/48h) to exactly 3 recipient wallets lacking a USDC associated token account — the @x402/svm-built tx has no create-ATA instruction, so such payments fail 100% deterministically while burning PayAI verify calls (~2,300/day). Agent-pay now probes the recipient's canonical USDC ATA via the rail RPC BEFORE any PayAI interaction: definitive absence → pending→settling CAS → terminal `failed`/`recipient_ata_missing`/`cap_exempt=true` (provably-no-broadcast convention), zero PayAI traffic; probe error → indeterminate → fails OPEN to the unchanged flow. SAP bounty release reuses the same helper (definitive absence → existing retryable `payai_unavailable`, so bounties pay once the ATA exists). No ATA auto-creation (founder funds the 3 wallets via Coinbase USDC sends, which create the accounts). Notable retraction upstream of this fix: the earlier "senders need SOL for gas" theory was DISPROVEN (facilitator sponsors gas; same 0-SOL senders settled 792/12h). Codex implemented / Opus reviewed + re-verified (tsc 0; agent-pay + agent-pay-resume + payai-conformance 68/68). **PARITY:** identical guard for human + agent payments; SAP worker payouts same probe. **SCHEMA:** `synced` — no migration (`@solana/spl-token` added as a direct api dep, already in the tree transitively). AWAITING staging deploy verify → staging→master promotion → prod live-verify (`recipient_ata_missing` rows appear; sim-fail verifies → ~0).
- **2026-07-24 (session SAPmcp) — CURRENT: `staging` = `4e56e99a` (docs correction included).** Adds (1) the **agent-pay stale-pending AUTO-EXPIRY** — `runAgentPayResumeTick` now closes provably-no-money pending rows >24h (all four money-evidence fields NULL) to terminal `failed`/`stale_pending_expired` BEFORE the stale-pending alert count, so the Telegram alert pages only for survivors carrying money evidence. Root incident: hundreds of forever-pending rows/day; the count-change-dedup'd alert re-fired constantly (founder paged at 02:47 with pendingCount 355). (CORRECTION same day: the initial "senders lack SOL for gas" theory was DISPROVEN — the facilitator sponsors gas and the same 0-SOL senders settled 792 payments/12h; intermittent sim-fails are a separate unpinned issue and land in `failed`, not pending.) Ops closed 6,308+36+373+4 such rows manually on 07-23/24; a temporary 5-min host cron on prod holds the line until THIS deploy (remove `/root/stale-pending-cron.sh` + crontab entry after prod verify). Codex implemented / Opus reviewed + re-verified (tsc 0; agent-pay + agent-pay-resume 48/48 green). `cap_exempt` untouched (failed not in `COUNTED_DAILY_CAP_STATUSES`). (2) **CLAUDE.md untracked from git** (founder directive 2026-07-24: `.claude` + `CLAUDE.md` off GitHub across all repos; `.claude/` was already ignored) — the file STAYS on disk in every checkout and remains the canonical project instructions; pulls of this commit delete the working copy in other checkouts, restore from `<main-checkout>/.claude/backups/CLAUDE.md.backup-2026-07-24`. NOTE: git HISTORY still contains old CLAUDE.md revisions (public repo) — full purge = filter-repo + force-push, founder decision pending. **PARITY:** ops-alerting + repo hygiene only; no player/agent surface change. **SCHEMA:** `synced` — no migration. AWAITING staging deploy verify → staging→master promotion → prod verify → cron removal.
- **2026-07-23 (session agentPickr) — CURRENT: `staging` = `60bf8d28`.** Adds the **agent-pay daily-cap "no-broadcast" accounting fix** on top of the cove house-settlement fix + Spec-4 bulk reconciler already on staging. Bug: `admitPending()` counted every `reconcile` row toward the daily send/receive caps, so the 2026-07-22 PayAI free-tier outage (~5,295 no-money reconcile rows/day) 429-locked all active agents — a defect that would hit real users identically. Fix: additive nullable `agent_payments.cap_exempt` (migration `0045`); the cap aggregate excludes only rows where it `IS TRUE`; the bit is set ONLY at provably-pre-broadcast points (pre-settle failures unconditionally; a signature-less settle rejection ONLY when its `errorReason` is on a NARROW allowlist — currently just `free_tier_exhausted`). Everything ambiguous (unknown/absent/compound reason, transport error, any signature) still counts — fail-closed. Hardened via the copus-max pass (Codex gpt-5.6-sol adversarial + Opus 4.8 @ max) which caught that the first cut over-trusted any signature-less rejection (PayAI's client collapses transport failures into blank signatures too). Verified pre-push: apps/api tsc 0; `bun test` 94/94 GREEN across agent-pay + agent-pay-resume + x402-payai + x402-payai-auth + x402-payai-conformance (allowlist exemption + unknown/compound/message-spoof/2xx-unknown/signature-bearing/transport all pinned). Caps VALUE ($100/day send+receive, $25/tx) were already set on both boxes 2026-07-23 (env-only). **PARITY:** human + agent settlement identical; accounting-only. AWAITING staging deploy verify → staging→master promotion (CI migrate gate applies `0045` to prod).
- **2026-07-23 (session SAPmcp) — CURRENT: `staging` = `72ac68b1`.** Adds the **autonomous Cove HOUSE/hosted settlement fix** (`play_cove_game` now settles for our own fleet agents like Coralia via the server-owned `openclaw_bots.is_house=true` avatar binding — previously dropped "invalid or expired agent session"; 0 autonomous-cove ledger rows ever) + the **autonomy-driver decoupling** (`agentAutonomyDriver.start()` moved out of the `ensureHouseAgent()` try/catch so a seed failure can no longer silently stop ALL autonomous agents), on top of the reef-race R18e grand-item-VFX round (`e4032d9f`, presentation-only). Verified pre-push: real-Postgres settlement suite 14/14 GREEN vs staging DB (incl. house slots+blackjack, conservation, all guards + negative cases), api build from source exit 0, tsc 0. **PARITY:** human = authed Cove REST; agent (connected) = `X-Clawville-Agent-Session`; agent (house/hosted) = server-side avatar binding. No schema/wire/PROTOCOL_VERSION change. AWAITING staging deploy verify + mock-Hatcher harness + live staging house-agent cove test, THEN staging→master promotion. **NOTE:** the CURRENT-STATE detail below this line is stale (2026-07-10) and predates ~10 intervening promotions (#229/#231/#232/#235/#236/#237 + cv-meridian) — trust the DEPLOY LOG entries and `git log origin/staging`, not the 07-10 prose.
- **Branch / commit:** `staging` = `393d3950` + the L-3 push riding this diff (**composition rail LIVE-PROVEN: pricing-tier 6148 fix `15158557` + L-1/L-2 liveness fixes `393d3950`, both containers DEPLOY-OK; L-3 payai/V1/alert in this push** — see the newest DEPLOY LOG entries). **PROD = `master` `60f5769a` (PR #194, the SAP B1 stack only)** — prod SAP flags confirmed OFF (`/api/sap/status`: enabled:false, escrowEnabled:false, dryRun:true). The composition rail + vCLAW-v14 are NOT yet on prod (staging-only; promotion PR pending founder sign-off).
- **Updated:** 2026-07-10 ~23:10 — the composed bounty rail is **LIVE-PROVEN on staging devnet, twice**: (1) 11-USDC e2e GREEN (bounty `d0713537`, conservation exact — the pricing-fix proof); (2) **wedge-and-self-heal e2e GREEN** (bounty `d33953c5`: deliberately reproduced the settle-rent wedge with a drained house wallet → gate row stayed retryable `status='open'` (L-2 live) → the resume sweep settled + finalized + paid it with ZERO manual intervention (L-1 live) → `SELF-HEAL-VERDICT:PAID`; conservation exact: hunter +10.000, creator net −10.05). The prior "e2e blocked on devnet funding" note is RESOLVED (rescue-funded the actors). ⚠️ Devnet SOL across all actor wallets is now LOW (~0.005 total incl. rescue ~0.003) — top up before the next SOL-heavy round.
- **⚠️ Staging SAP flags are ON + SAP_DRY_RUN=false (deliberate PayAI-leg test config)** — so the NEW V2 release routes (`/api/sap/escrow/v2/{open,settle,finalize}` + `/api/sap/agent/pricing`) are LIVE on staging against DEVNET play money for authed non-guest users. Prod keeps everything OFF (enabled:false, dryRun:true).
- **Health:** 🟡 staging — S2 v3 pushed. **CORRECTION to the prior entry:** the "rest 0.89 / deepest 0.63 × 168.75 = 44wu lift verified" claim was WRONG — a headless integration test (real VRM + `_emotes.glb` + real `retargetMixamoClip`) proved the `squat` clip is rotation-only (raw hips Y flat constant 104.226, ZERO descent), so the v1 `getSquatGroundLift` was a **no-op** and bug #1 was NOT fixed by what shipped at `0effc747`. v3 replaces it with FOOT-GROUNDING (plant lowest foot on floor → body lowers into the pose). Build green (next build exit 0). Depth is shallow (~2.3wu, the clip's knee-bend) — direction fixed, depth needs founder eyes + maybe a re-baked clip. Staging carries, ALL awaiting founder sign-off (Rule E4): **S2 jump** (cold-test idle/walk/run + mobile — feet planted + body settles DOWN, no midair tuck, no slide, run keeps running; flag if the crouch is too shallow), **S1 loading bar** (cold-reload), **blackjack hardening**. PROD unchanged.
- **SCHEMA:** this push adds migrations `0023a_sap_deposit_requests.sql` + `0023b_sap_escrow_withdrawals.sql` + `0023c_sap_withdraw_idempotency.sql` (idempotent additive; apply to STAGING via the CI migrate job on this push). `prod-migration-pending: 0022a-e + 0023a-c` — the staging→master promotion's CI migrate gate applies them to prod.
- **DB isolation:** staging = Supabase `mtpixvtclsjqjguouxes`; prod = `wheuidgiyyccqyoppxoa`. **Separate DBs since 2026-06-16** — staging writes no longer touch prod.
- **Both GH secrets set:** `STAGING_DATABASE_URL` + `PROD_DATABASE_URL` (session pooler :5432).
- **Security follow-up (non-urgent, founder-acked):** prod DB password leaked into agent transcripts during this session (see log) + was in `packages/database/.env.local`. Rotate when convenient (Supabase + prod Coolify env + `PROD_DATABASE_URL` secret together).

---

## DEPLOY LOG (newest first — keep ~15 entries, trim the tail)

### 2026-07-27 (repo-scrub session, push 7) — Clawville Display: a REAL font built from the logo lettering
- **What:** the logo lettering was AI art with no font behind it (founder confirmed). Built one: gpt-image-1 generated a full A-Z/a-z/0-9 glyph sheet styled on the logo (OpenAI per founder, no Gemini), then a local pipeline (yellow-fill mask -> per-glyph slice -> potrace vectorize -> fontTools CFF assemble) produced `branding/assets/fonts/ClawvilleDisplay.otf` + `.woff2`. Sheet + build script committed for regeneration. Caveats doc'd: display-quality (no kerning), lowercase z synthesized from Z, digits spanned two sheet rows. Brand board renders a live specimen.
- **SCHEMA:** `synced`. **PARITY:** n/a.

### 2026-07-26 (repo-scrub session, push 6) — OFFICIAL logo + animated banner pulled from DexScreener
- **What:** founder corrections: the town "More Than A Game" scene is an ARTICLE PROMO (not official), and the pirate OG is ALTERNATE art. THE official logo (200x200, mascot-in-surf under the sign) + THE official ANIMATED banner (600x200 GIF, 144 frames) pulled as originals from the $CLAWVILLE DexScreener profile (mint `Epht7Fw4Sgh6fdcJj6afWXuNcAUmLLMc3MSthUqELiZA`; re-pull recipe documented in BRAND.md §5). Static banner copy added; BRAND.md hierarchy + inventory + board updated; "More Than A Game" demoted to promo copy.
- **SCHEMA:** `synced`. **PARITY:** n/a.

### 2026-07-27 (session notDev) — P1c: presence continuity + nav hardening + soak gate (merge `f9786879`)

- **What changed:** cove crossings keep your body in the shared world ("at the Cove" tag, layout-owned streams, protocol v40); durable first-crossing navigation + history bounded at 4; new 60-loop soak gate.
- **What broke + root cause + fix:** the soak's first three runs FAILED honestly — four real leaks (cove/world lights+labels+hotspots remounting every crossing; App Router history growing 2 entries/crossing; incomplete warmup allocating late) — all fixed, scene inventories now diff to exactly zero. Residual ~0.45 MB/crossing heap-snapshot-named as three r185 WebGPU-internal bindGroups caching (not ours; follow-up tracked on r186+ upgrade). Codex final leg died on provider capacity ("model at capacity") after committing gate calibration — Fable ran the full gate suite personally. Merge conflicted with the same-day autonomy-walk + mascot pushes in the 3 canonical doc headers — resolved keeping both audit entries.
- **Who it's for:** every player/agent crossing between town and the Cove (no more vanishing bodies); the P2-P4 stage migrations inherit the soak gate.

### 2026-07-26 (repo-scrub session, push 5) — cinematic pirate mascot lands in the kit + banners
- **What:** founder supplied two new official art files (fetched over SSH from the laptop): `clawville-logo-og.jpg` (square OG card, cinematic pirate lobster hoisting the sign) + `clawville-logo-banner.jpg` (town scene carrying the "More Than A Game" tagline). Derived `mascot-pirate-cutout.png` (AI background removal + cleanup) and made it the DEFAULT Register-B banner hero across all three templates (the sign in the hero replaces the separate logo badge). BRAND.md + brand board updated (inventory, mascot section, tagline, open-flag 5 partially resolved).
- **Who it's for:** the SAP/Meridian announcement banners now carry marketing's cinematic mascot.
- **SCHEMA:** `synced`. **PARITY:** n/a.

### 2026-07-26 (session copusMax, push 1) — autonomous-agent directed-walk fix (`agent-autonomy-driver.ts` + `npc-simulation.ts` + `pathfinding.ts`)
- **What:** house/autonomous agents took ~10 min for a ~1-2 min walk. A single collider graze made the sim discard the whole route + destination + go idle; the driver then idled a flat 120 s and burned a full LLM re-decide, repeatedly. Fix: (1) a destination-stamped route is DIRECTED (tracked by path-array WeakSet) and is never self-abandoned on a graze — it falls through to the existing collision-free wall-slide clamp (ZERO findPath in the 200 ms tick — that A* measures 165-258 ms/call and shares the Bun loop with SSE/HTTP); directed routes are also excluded from ambient planning/idle-conversation so a server-managed connected-agent route isn't hijacked. (2) the give-up deadline scales to route length (120 s floor / 180 s ceiling, 5 min episode cap). (3) the 30 s driver re-routes a wedged/overdue trip to the same goal WITHOUT an LLM call (≤2 free re-routes) and now runs the walking phase without a warm runtime.
- **What broke + fix:** v1 put findPath in the sim tick behind a "2 per tick" budget — Codex adversarial review REJECTED it (measured findPath 165-258 ms; one call exceeds the 200 ms tick; failed searches uncharged; the driver bypassed the counter). v2 moves ALL pathfinding to the 30 s driver, so the sim does zero A* (asserted by a pathfinder call counter).
- **Who it's for:** any autonomous/house agent (Coralia) or connected Hatcher agent sent to a building/cove/kelp. Human movement is untouched.
- **Verified:** apps/api tsc 0; npc-directed-route 15/15; agent-autonomy-walk-budget 17/17; full apps/api service suite 1742 tests, 0 fail. Docs same-diff (ARCHITECTURE + GameFeatures both copies + 3dStructure line 505 corrected).
- **SCHEMA:** `synced` — no migration, no wire/PROTOCOL_VERSION change.

### 2026-07-26 (repo-scrub session, push 3) — SAP/Meridian announcement banners + banner template system (`branding/graphics/`)
- **What:** three Register-B banner templates at X-post size (1965x800), built per the BRAND.md §7 recipe and browser-verified: `banner-agents-pay-agents.html` (Electric Blue, LIVE: gasless x402 USDC payments, dual facilitator failover), `banner-agent-economy-live.html` (Neon Lime, LIVE: escrowed bounties, staked agents, provable payouts), `banner-agent-passport.html` (Champagne Gold, COMING SOON: on-chain identity + reputation, uses the locked keeper phrases). Supporting: bundled OFL webfonts (Anton/Barlow woff2), derived mascot-only transparent cutout (sign masked out + hole-healed). Exports = open template at 1965x800 viewport, screenshot.
- **Who it's for:** founder marketing push on the agent-economy work; future sessions reuse the templates for any Register-B announcement.
- **What broke:** nothing; static assets only. Copy follows brand rules (no em dashes, vCLAW naming n/a, partner names text-only pending their nod).
- **SCHEMA:** `synced`. **PARITY:** n/a.
- **Push 4 (`4e1555f3`) — founder rulings codified in BRAND.md/brand-board:** domain is ALWAYS `clawville.world` (we do not own .com; the .com in the Recap exemplar is an error) · naming PayAI/Meridian/OOBE/Covenant in graphics needs NO sign-off (we implement their software and are partnered). Docs-only.

### 2026-07-26 (repo-scrub session, push 2) — CLAWVILLE BRAND KIT added (`branding/`)
- **What:** first-ever repo brand kit, built from the marketing team's Drive asset folders (founder-provided): `branding/BRAND.md` (canonical rules: the two visual registers, measured hex palettes, typography stand-ins, logo/mascot rules, the broadcast-banner recipe, copy rules) + `branding/brand-board.html` (visual one-pager, browser-verified) + 23 curated assets under `branding/assets/` (logos incl. mono SVG + PSD source, mascot art, beach banners, 9 stickers, 3 published marketing exemplars, 2 video clips). Also commits the previously untracked `docs/brand-language.md` (locked keeper phrases) which BRAND.md references. `early-ideas.jpg` deliberately NOT committed (contains third-party game characters; Drive-internal reference only).
- **Who it's for:** every future session producing outward-facing graphics/copy; closes the "repo has zero exposure to marketing's style" gap (founder 2026-07-26).
- **What broke:** nothing; assets-and-docs only, no app surface. ~21MB repo addition.
- **SCHEMA:** `synced`. **PARITY:** n/a (no feature surface).

### 2026-07-26 (repo-scrub session) — GIT HISTORY PURGE + .gitignore guard pushed to staging
- **What:** completed the founder's 2026-07-24 scrub directive: `git filter-repo` removed `CLAUDE.md`, `AGENTS.md`, `.claude/`, `*.itachi-bak` from ALL 5,872 commits on ALL refs, force-pushed (`--mirror`). GitHub API verified 0 commits touching any purged path. This push adds `.gitignore` entries (`AGENTS.md`, `*.itachi-bak`) so nothing re-commits them, plus this ledger entry. AGENTS.md working copies: restore from `<main-checkout>/.claude/backups/AGENTS.md.backup-2026-07-26`.
- **What broke / caveats:** nothing deployed changed (trees byte-identical; no rebuild fired, none needed). KNOWN LIMITS: (1) pre-rewrite SHAs remain fetchable as orphaned objects until GitHub GC — complete erasure needs a GitHub Support request (founder account); (2) 2 public forks of ClawVille retain the old history independently; (3) the `ItachiDevv/milady` fork still carries our CLAUDE.md history — deletion needs `delete_repo` token scope or a founder click (repo settings → Delete).
- **Who it's for:** repo security hygiene (public repo, money-handling app).
- **SCHEMA:** `synced` — no migration. **PARITY:** n/a (no feature surface).
- **What:** the reload-on-return fix track, slices 0a/0b/1a (full plan + execution ledger: `docs/persistent-world-canvas-plan-2026-07-24.md`; per-slice briefs+notes in `docs/world-stage-*`). `/game` rendering moved onto ONE persistent canvas in the new `app/(world)/` route-group layout (parity cutover — behavior should be IDENTICAL; the loading-screen-free returns land in P1b when `/cove` joins). Includes the P0b cove-on-WebGPU compat spike (VERDICT: compatible, zero material fixes; real interior GLB proven under both backends), consolidated renderer crash-recovery (device-loss/context-lost bounded recovery, no reload loops), byte-accurate VRAM ledger, and the automated 100-transition regression probe.
- **Who it's for:** every web player (rendering substrate); no server/API/economy change whatsoever (web-only diff).
- **Broke + root cause + fix (pre-push):** (1) Codex's first P0b/P1a runs hit the Windows sandbox write-degradation + a host process-spawn exhaustion (`0xC0000142` after a Turbopack worker crash) — work recovered from disk both times, orphan process pile swept, everything re-verified independently before commit. (2) P1a brief carried a stale doc DPR value (`[0.5,0.65]`); Codex followed the brief; review corrected the stage to the LIVE legacy constants `[0.55,0.7]` (live code > docs precedence). Nothing broken post-verification.
- **How verified:** build 0 · web tsc 0 · probe 102/102 both backends (fresh JSONs committed) · `/game` no-store header intact · real-browser: world+NPCs+HUD render on the stage, ~58 FPS dev box, HUD clicks + canvas pointer events both work, `/game`↔`/perf/stage` round trip clean, `/arena` legacy smoke green.
- **SCHEMA:** `synced` — no migration.

### 2026-07-23 (session agentPickr) — AGENT-PAY DAILY-CAP NO-BROADCAST ACCOUNTING FIX pushed to staging (`60bf8d28`)
- **What:** the daily send/receive cap aggregate in `admitPending()` counted EVERY `agent_payments` row in `reconcile`/`pending`/`settling`/`settled` — including terminal `reconcile` rows that provably moved no money. During the 2026-07-22 PayAI `free_tier_exhausted` outage that meant ~5,295 no-money reconcile rows/day burned the caps and `daily_cap_exceeded` (429) locked every active agent until 00:00 UTC. Real users hit this identically. Fix: additive nullable boolean `agent_payments.cap_exempt` (migration `0045_agent_pay_cap_exempt.sql`); aggregate excludes rows where `cap_exempt IS TRUE` (`IS NOT TRUE` keeps NULL/false counting → every historical + uncertain row still counts). The exempt bit is set ONLY at provably-pre-broadcast points: pre-settle failures (malformed header / config error / verify threw / verify invalid / verify-only — `/settle` never called) unconditionally; a settle rejection (thrown SettleError OR 2xx `success:false`) ONLY when it has NO tx signature AND its `errorReason` is on a narrow allowlist (currently `free_tier_exhausted` only — PayAI's documented quota gate). `custodial-x402` inherits the narrowed bit.
- **How verified (pre-push, orchestrator-run not agent-reported):** apps/api tsc exit 0; `bun test` **94/94 GREEN** across agent-pay, agent-pay-resume, x402-payai, x402-payai-auth, x402-payai-conformance. New conformance cases pin the fail-closed direction: allowlisted `free_tier_exhausted` (explicit + message-fallback) → exempt; unknown / `free_tier_exhausted_maybe` / no-reason / 2xx-unknown / signature-bearing / generic transport error whose message merely mentions the phrase → all COUNT.
- **Review:** copus-max pass — Codex gpt-5.6-sol adversarial critique (found the first cut exempted ANY signature-less rejection = unsound, since PayAI's own facilitator client collapses transport/non-2xx failures into `success:false, transaction:''`) + Opus 4.8 @ max reviewer (same find). Both converged on the narrow allowlist; Codex implemented the hardening, orchestrator re-ran every gate + read the diff before commit.
- **Broke:** nothing observed pre-deploy. Rebased onto `origin/staging` (only ARCHITECTURE.md conflicted — both sides added 2026-07-23 dated entries; kept both), clean fast-forward push.
- **SCHEMA:** `prod-migration-pending: 0045_agent_pay_cap_exempt.sql` — additive nullable column, no backfill; the staging→master CI migrate gate applies it to prod.
- **For:** the founder + real users — a terminal no-money settlement failure no longer eats a day's cap. Caps VALUE ($100/day send+receive = 10,000 vCLAW/day earn, $25/tx) already env-set on both boxes earlier 2026-07-23. No `PROTOCOL_VERSION` bump (wire/verb/whitelist unchanged; not the Hatcher surface). AWAITING staging deploy verify → staging→master promotion.

### 2026-07-23 (session SAPmcp) — AUTONOMOUS COVE HOUSE/HOSTED SETTLEMENT FIX pushed to staging (`72ac68b1`)
- **What:** the autonomous `[ACTION: play_cove_game(game,wager)]` verb settled only for CONNECTED partner/magic-link agents; our own HOUSE/hosted fleet agents (Coralia et al.) were structurally locked out — settlement resolved the subject solely via `resolveAgentSession(sessionId)`, but house agents run a deliberately non-ledger, unbound local `oc-` session absent from that registry, so every autonomous house cove play dropped with "invalid or expired agent session" (fleet-wide autonomous-cove ledger rows: 0, ever — a Rule E5 parity defect in a money feature). Fix: new `autonomous-cove-agent-binding.ts` resolves the ledger binding from the server-owned `openclaw_bots(agent_id=<server-attributed agentId>, is_house=true)` row + its non-guest owner + unique avatar; no house row → falls through to the connected resolver UNCHANGED. Wired into the outer world executor + both slots/blackjack adapters + slots' private open/close/spin context. House ledger avatars are intentionally `is_active=false` (their `ocb-` body is the sole world presence) → the money txns permit that ONLY after locking the exact avatar row FOR UPDATE and re-proving the exact `(agent_id,user_id,is_house=true)` + non-guest owner in the SAME transaction. Also decoupled `agentAutonomyDriver.start()` from `ensureHouseAgent()`'s try/catch (a seed throw previously skipped driver start, silently halting ALL autonomous agents).
- **How verified (pre-push):** real-Postgres settlement suite **14/14 GREEN vs staging DB** (house slots+blackjack settle through the world action; conservation invariants; post-resolution deactivation reject; changed-binding reject; non-ledger/unbound reject; rate gate refuses req 61; daily cap; blackjack 4× worst-case pre-shoe cap; owner-scoped replay across session/shoe rotation; unchanged non-house drop). This closes the exact green-tests/broken-real-path gap that hid the bug (the old suite mocked `resolveAgent`). Merged-tree api build from source exit 0; tsc 0-in-touched. Two Codex adversarial passes + manual review.
- **Broke:** nothing observed pre-deploy. First push hit non-fast-forward (reef R18e VFX `e4032d9f` had landed on staging mid-work) → merged origin/staging (no overlap with cove files) → pushed clean. Local git push needed the env `GITHUB_TOKEN` PAT as one-shot Basic auth (gh keyring token + all local SSH keys are itachi-memory-deploy-scoped only, no ClawVille push).
- **SCHEMA:** `synced` — no migration (settlement-resolution only; no DDL).
- **For:** the founder — hosted fleet agents (Coralia) can now actually settle a cove hand autonomously. AWAITING staging deploy verify → mock-Hatcher harness → live staging house-agent cove test → staging→master promotion → live prod Coralia confirmation. No `PROTOCOL_VERSION` bump (verb/params/wire unchanged).

### 2026-07-10 — MAINNET-ON-STAGING ENABLEMENT (IN REVIEW, UNPUSHED) — the reviewed SAP mainnet code-gate flip
- **What:** the founder-corrected validation ladder's next rung — devnet-on-staging (done) → MAINNET-on-staging. (1) Flipped `SAP_ALLOW_MAINNET` false→true in `sap-config.ts` (LOCK 1; the deliberate config-review event the gate was built to require). Two-lock preserved: a box stays devnet unless it ALSO sets `SAP_CLUSTER=mainnet` (LOCK 2), real funds move only with `SAP_DRY_RUN=false` + enable flags. (2) `USDC_BOUNTY_REWARD_MIN` (default 10, floor 1, USDC rail only, CT floor unchanged) so the funded smoke posts a 1-USDC bounty. (3) house provisioner confirmed cluster-agnostic + mainnet-cost header note (0.1 SOL stake + ~0.055 SOL rent, both recoverable; true burn ≈ 0.5% fee + tx fees).
- **CRITICAL pre-existing bug FIXED in this slice:** the live-send mainnet-broadcast guard (`assertNotMainnetGenesis`) compared `getGenesisHash()` (44-char) against `SOLANA_MAINNET_GENESIS_HASH` which held only the **32-char CAIP-2 truncation** — so it NEVER matched and SILENTLY FAILED OPEN (a devnet box on a mainnet RPC would NOT have been refused). Corrected to the full genesis hash `5eykt4UsFv8P8NJdTREpY1vzqKqZKvdpKuc147dw2N9d` (verified vs Solana RPC docs; first 32 chars == the CAIP-2 already in x402-payai.ts), pinned by test. This mattered far more once mainnet boxes exist.
- **Verified:** new `sap-config.test.ts` pins the mainnet USDC mint (`EPjFW…Dt1v`), the devnet mint, the full genesis hash, and the two-lock gate (mainnet no longer throws with LOCK 1 true; a box without `SAP_CLUSTER=mainnet` stays devnet). `USDC_BOUNTY_REWARD_MIN` resolver pinned (default/override/clamp). Full suites + tsc counts in the review report.
- **Env for the mainnet box (NOT applied here — ops sets in Coolify):** `SAP_CLUSTER=mainnet` + a paid `SAP_RPC_URL` (Helius mainnet, REQUIRED) + `USDC_BOUNTY_REWARD_MIN=1` + `SAP_DRY_RUN=false` + the enable flags, on the funded smoke box only. Prod is the LAST rung.
- **For:** the founder — mainnet is now unlocked at the code layer (reviewed); staging can run a real 1-USDC bounty once ops sets the mainnet env on the funded box. Full review chain (Fable + adversary) before commit.

### 2026-07-10 — L-1/L-2 SHIPPED (`393d3950`, api DEPLOY-OK after tinker retrigger) + LIVE WEDGE-SELF-HEAL PROOF + L-3 payai/V1/alert (this push)
- **What (this push, L-3):** the same wedge fix applied to the two remaining terminal-'failed' writers + the missing ops signal. **L-3a (payai LEG-2 hunter payout):** `settleJob`'s payai branch now splits by `broadcastUnknown` — `false` (verify-stage failure; VERIFIED end-to-end by orchestrator + adversary that `/settle` is provably never called on that path, incl. the settle-throw path which explicitly preserves `isValid:true`) restores `preClaimStatus` retryable; `true` (verify passed → `/settle` attempted) stays terminal `failed`, NEVER auto-retried — double-pay not constructible. **L-3b (V1 chain settle):** same `broadcast` split (latent, off the composed path). **L-3c:** persistent-wedge `alertError` — a vault_held+approved bounty that keeps failing on the crank previously paged NOTHING; now pages critical, deduped 1h/bounty, throttle recorded on DELIVERY (a failed alert retries next pass), Map pruned to currently-swept rows each pass. Adversary verdict SHIP-WITH-NITS (both nits applied in-diff).
- **LIVE WEDGE-AND-SELF-HEAL PROOF (ran on `393d3950` on staging devnet — the L-1/L-2 acceptance test):** deliberately reproduced the d0713537 incident — drained the house wallet to 0.001 SOL (in-container custodial sign) → bounty `d33953c5` (10 USDC, lt2→lt1; note the create republished the house tier 11→10, a THIRD live pricing-fix proof) → approve failed at settle sim exactly like the incident → **gate row stayed `status='open'` + `settleSimFailure:true` (L-2 LIVE — the state that was terminal `failed` that morning)** → restored house SOL → **the resume sweep self-healed it: sweep 1 settled → awaiting_finalize → sweep 2 finalized + paid → `SELF-HEAL-VERDICT:PAID`, ZERO manual intervention (L-1 LIVE, real EXISTS sweep on the real DB).** Conservation exact: hunter +10.000 (0.445→10.445), creator net −10.05 (12.1208→2.0708), house 0 net, vault 0.
- **Verified (L-3):** SAP suite 173/0 (+4 L-3a/b disposition tests incl. no-re-drive pins, +2 L-3c alert tests); routes 210 pass / 3 pre-existing Hatcher fails; tsc 0-in-touched (8 pre-existing env/merge errors unchanged). Frozen wire builders untouched; PROTOCOL_VERSION unchanged.
- **Broke:** nothing new. The recurring Coolify api-container flip flake hit BOTH pushes (`ae21f753`, `393d3950`) — auto-retrigger via tinker recovered both; the waiter script now retriggers inline.
- **Deferred (candidates for an L-4 slice, only if ops noise proves real):** terminal quarantine/backoff for permanent wedges; the vault_held↔reconcile oscillation double-page; L3022 (V1 refund pre-broadcast-terminal, report-only).
- **For:** the founder — the composed rail's failure modes now self-heal and page ops; prod flags stay OFF; promotion PR awaits your sign-off.

### 2026-07-10 — bounty pricing-tier 6148 FIX + LIVE 11-USDC composed e2e GREEN (staging push `15158557`, HEAD `ae21f753`, both containers DEPLOY-OK) + L-1/L-2 liveness fixes (shipped later same day as `393d3950` — see the entry above)
- **What (pricing fix, PUSHED `15158557`):** `openComposedBountyEscrow` now (re)publishes the house pricing tier at each bounty's EXACT price (`updateAgentPricingUsdc`, tier `bounty-usdc`, `pricePerCall = reward×10^6`) under a per-house keyed mutex BEFORE opening the LEG-1 V2 vault. Root cause: `create_escrow_v2` requires the escrow `price_per_call` to match a tier in the worker's on-chain pricing_menu, but the house published only a fixed NOMINAL 1-USDC tier → every composed bounty whose reward ≠ the last manually-set tier failed `PricingTierNotFound 6148` at create. Invisible to the mocked suite; caught by the live e2e. HEAD now `ae21f753` (merge of origin/staging).
- **LIVE COMPOSED E2E — GREEN end-to-end on devnet (proves the pricing fix + the whole two-leg rail):** bounty `d0713537` (11 USDC). LEG 1 vault `BTBMeg77…` opened creator→house at price 11_000_000 (tier matched — no 6148) → settle `2dsAZgTc…` (principal reserved + 0.5% fee) → finalize `2KTZHCd5…` (principal → house). LEG 2 payout escrow `46jmx2eR…` → x402 house→hunter `5t9nkso9…`. **Conservation EXACT: creator net −11.055 USDC (reward + 0.5% fee + headroom, dust reclaimed); hunter +11.000 exact.**
- **Two LIVENESS bugs the e2e caught (both MONEY-SAFE — no wrong fund movement — but they WEDGED the approved bounty until manual DB surgery); FIX IN REVIEW, NOT YET PUSHED:**
  - **L-2 (gate, `escrow-gate.ts settleJobV2`):** the generic post-claim failure arm parked a PROVABLY PRE-BROADCAST failure (`broadcast` falsy — nothing hit the wire; the live cause was the house wallet unable to front the PendingSettlement PDA rent, a transient fixed by a SOL top-up) as terminal `status='failed'`, which the settle claim then refuses (`job_not_open — job is failed`). FIX: the pre-broadcast arm now RESTORES the row to `preClaimStatus` (retryable), exactly like the adjacent replay-signal branch; `broadcast:true` still quarantines `settle_unknown` UNCHANGED. Money-neutral (reservation/fee columns identical; only the status label flips terminal→retryable, so `escrowFundsLedger` sums byte-identically).
  - **L-1 (worker, `bounty-composition-worker.ts`):** the resume crank's `RESUMABLE_STATES` excluded `vault_held`, so an approve whose settle failed pre-settle (leaving `vault_held` WITH an approved winner) was never re-driven — the route's own "retryable via an ops crank" note pointed at a crank that didn't sweep it. FIX: the sweep now ALSO picks up `vault_held` bounties carrying an APPROVED attempt (`EXISTS(approved attempt)` query filter + `no_winner` backstop; hunter resolved FROM the approved attempt row, never from input). Double-book-safe: approve + sweep both feed `bookComposedBountyPaid` the same `'vault_held'` CAS prior, and every settle leg is at-most-once. Together the two fixes make the wedge SELF-HEAL (gate restores retryable → next sweep → paid).
- **Verified:** SAP suite 167/0 (incl. +2 L-2 gate + 3 L-1 composition tests; also repaired a PRE-EXISTING `escrow-v2-gate.test.ts` mock that the `ae21f753` merge broke — the merge added a `withdrawEscrowV2Usdc` import to escrow-gate but not to the test's partial sap-client mock, red at module load). Routes 210 pass / 3 pre-existing Hatcher fails. tsc: 0 errors in touched files (8 total, all pre-existing/environmental from the merge: 7 guest-leaderboard-test + 1 wager-program). Frozen wire builders (`sap-escrow-v2.ts`/`executeTx`/V1) untouched.
- **Broke:** the `ae21f753` merge left `escrow-v2-gate.test.ts` red at module load (partial mock missing the merge-added export) — repaired in this L-1/L-2 diff. No prod impact (rail dark).
- **For:** the founder — the composed bounty rail is now LIVE-PROVEN (11 USDC, conservation exact) AND the two liveness wedges the e2e surfaced are fixed. Pricing fix is on staging (`15158557`); the L-1/L-2 fix is in review, unpushed. Prod flags stay OFF.

### 2026-07-10 — bounty COMPOSITION RAIL + vCLAW-v14 rename (staging push, DARK; migration 0024)
- **What (composition rail):** the founder-locked bounty spec — "escrow through SAP, settle via x402/PayAI" — built as a two-leg composition (11 commits `30ca1c55`→`4b4b6b3e`): LEG 1 = a SAP V2 vault opened creator→HOUSE at bounty CREATE (custody from posting; house = the fixed known worker since the program seeds every escrow with a worker key and the hunter is unknown at post), LEG 2 = on the creator's PASS verdict, the vault settles+finalizes to the house wallet then ONE x402 exact-scheme USDC payment house→hunter through the PayAI facilitator. Auto-reclaim leg 1d returns the ~0.5% deposit-headroom dust to the creator (net cost = reward + 0.5%). FAIL/cancel → creator refunded. States: vault_pending (fail-closed create-crash sentinel) → vault_held → paid | awaiting_finalize→(crank)→paid | reconcile_payout_failed | failed. DARK-gated (SAP_ENABLED + SAP_ESCROW_ENABLED + SAP_USDC_ESCROW_ENABLED + SAP_PAYAI_SETTLEMENT_ENABLED); a finalize/payout crank (SAP_BOUNTY_RESUME_POLL_MS) + near-instant window (SAP_BOUNTY_DISPUTE_WINDOW_SLOTS default 1). House live-provisioned on devnet (Coralia, wallet `EUCaGQYcSbM472NJTWn267qMbz1Zp2i2cfQvfyh3EUH2`, register/pricing/stake-0.1 confirmed).
- **What (vCLAW):** CT → vCLAW user-facing rename (82 files, PROTOCOL_VERSION 13→14, killed a hidden third `NT` alias that was the likely source of the leftovers the founder kept hitting). Copy-only; code identifiers/DB columns untouched.
- **Review:** Fable line-by-line every slice + impl1↔impl2 mutual cross-review (all APPROVED) + adversary money audit = SHIP-WITH-MEDS (conservation PROVEN sound across both legs + reclaim; no double-pay/refund/cross-pay). All 3 adversary findings fixed: MED-1 (a broadcast-unknown create no longer deletes a possibly-funded vault — `PRE_BROADCAST_NO_CUSTODY` allowlist, default-KEEP, fail-closed) + 2 LOWs (paid-CAS symmetry, atomic completion bump). Codex-verdict gate retired by founder earlier in the session — Fable owns ship verdicts.
- **Verified:** 159 SAP+composition tests / 0 fail; tsc 0 in touched files; frozen wire builders (`sap-escrow-v2.ts`/`executeTx`/V1) byte-clean over the whole range. mock-Hatcher harness GREEN on staging (client assertions all pass, protocol v14 on the wire); test signer evicted after. Both staging containers at `ce7eb0e1`; docs commit `4b4b6b3e` pushed.
- **Broke:** nothing — deploy quirk only (api container didn't flip on first webhook, retrigger via tinker fixed it, twice this session). The composed-bounty devnet e2e is NOT yet run (devnet funds dry — see CURRENT STATE).
- **For:** the founder — bounties now match your spec end-to-end (SAP escrow at posting → x402/PayAI settle → refund on fail), money-sound and dark. NOT on prod. Needs: your ship sign-off, a devnet top-up for the composed e2e, then the promotion PR (prod flags STAY OFF).

### 2026-07-10 — SAP B1 full-send — the 3 MED flip-gate fixes + 7 hardening rounds (staging push; 3 additive migrations `0023a-c`)
- **What:** the founder-authorized B1 full-send on `feat/sap-v2-release-gate`, 10 code/docs commits `12dd7874`→`52c924ac`: (1) **deposit idempotency** — `sap_deposit_requests` (0023a), UNIQUE (subject, requestId) + (escrowPda, amount) fingerprint, atomic claim-before-send, terminal replay (`replayed:true`), broadcast-unknown held reconcile-only, throw-park never deletes; (2) **withdraw truthfulness + idempotency** — `sap_escrow_withdrawals` (0023b) booked claim-first (0023c adds requestId, in_flight rows pessimistically HOLD funds), plus the settle claim clamps to LIVE physical-free = on-chain vault − escrow `pending_amount`; (3) **decoded replay-reconcile** — reconciles book the DECODED on-chain PendingSettlement, never caller amounts. Hardening on top, each round from a real finding: **index-PINNED settle sends** (`expectedSettlementIndex` — the program's seeds constraint enforces exactly-once per slot; kills the claim→send TOCTOU and the zombie-blockhash class, since validity is ~151 BLOCKS not wall-clock), fail-CLOSED unowned-pending guard (gateLivePending = {pending, finalizing} only — both broadcast-unknown quarantines excluded after two phantom-mask finds), monotonic-index gate on stale recovery (close_pending_settlement defeats PDA-absence proofs), stale settling/finalizing self-heal with QUARANTINE (no auto-restore — the restore→retry lifecycle dropped the slot pin), closed-escrow recovery quarantine, succeeded-deposits-count-as-funding, boot assertion pinning the stale-window/blockhash relationship.
- **Review chain (all findings fixed or explicitly flip-gated in docs/sap-integration.md B1):** Fable line-by-line every round + regress auditor (clean) + adversary ×5 passes (final: SHIP) + Codex ×3 fresh-session reviews (each found real double-pay classes; its last remaining item became the R10 quarantine). Founder retired the Codex-verdict GATE mid-session — review evidence stands, verdicts are Fable's now.
- **Flip-gates added (pre-real-funds, in the B1 block):** automated reconcile endpoint (unreconciled_onchain_pending / settle_slot_consumed / quarantined stale claims / out-of-band-finalized pendings) · per-pending PDA verification (the aggregate guard is blind to gate>on-chain; deployed settle is worker-signer-only per IDL — VERIFIED, not conditional) · stale-in_flight idempotency-claim reconciler (hard-crash, signature-less) · simulate-shapes.ts GREEN immediately pre-flip (standing).
- **Verified:** 130/130 SAP tests (85 baseline → +45) + tsc 33-error environmental baseline (0 in SAP files) — re-run independently by the orchestrator after every round. Wire builders (`sap-escrow-v2.ts` diff EMPTY over the whole range), `executeTx`, V1 executors byte-frozen. Staging flags remain ON+devnet (play money); **prod flags stay OFF** — the promotion changes code only, routes 503 on prod.
- **Broke:** nothing known at push time; deploy-verify + live staging probes follow in-session (this entry updated if that changes).

### 2026-07-09 — SAP release-retrofit session — V2 two-phase release through the escrow gate (staging push; 5 additive migrations `0022a-e`)
- **What:** the founder-authorized SAP release-path build, three reviewed slices on `feat/sap-v2-release-gate`: (1) `d6bd9410` — escrow-gate V2 lifecycle (openEscrowV2/settleJobV2/finalizeJobV2; settle reserves principal + 50bps fee INSIDE the advisory-locked claim → `pending`; permissionless finalize releases principal → `settled`; reconcile-only `settle_unknown`/`finalize_unknown`; chain replay guard honored — pending-PDA probe routes replays to finalize, never re-sends settle; V1/V2/payai cross-blocked; routes `/escrow/v2/{open,settle,finalize}` full E5 pattern); (2) `aabcb1f0` — `POST /api/sap/agent/pricing` (owner-signed `update_agent`, one Escrow-mode USDC tier, menu-REPLACE semantics; resolves the 6148 external-worker gap) + register `description` required non-empty (fixes on-chain 6024 fail-late); (3) `049f7d16` — 6153 derived: create requires `max(0.1 SOL, 50% × price×maxCalls)` comparing RAW token base units to lamports with NO conversion (source-cited escrow_v2.rs @ OOBE commit 55d29ede), and deposits cap at the create-time obligation (`balance + amount ≤ max_obligation`) — mirrored as FAIL-OPEN UX preflights with actionable "stake X more" errors. Both live smoke anchors (150k accept / 200k reject @ 0.11 SOL) reproduce under the derived rules.
- **Verified:** 85/85 SAP tests + tsc 33-error environmental baseline with 0 errors in touched files — both re-run independently by the reviewing orchestrator, not just the authoring agent. Wire builders/executeTx/V1 executors untouched (byte-parity pins hold). Review findings (2 MED, recorded in docs/sap-integration.md B1): v2-withdraw bypasses the gate ledger (reconcile before flip) + replay-reconcile must switch to decoded PendingSettlement amounts (staging e2e item).
- **Broke:** nothing — two deploy quirks only (api webhook didn't fire → queued via tinker; both verdicts DEPLOY-OK `793a03e1`).
- **E2E RESULT (same session, ~23:20–23:45Z) — GREEN end-to-end, real devnet sends via the live staging routes:** landtest1(worker)/landtest2(depositor, custodial `FSThy…7KD9` funded 0.03 SOL + 1 USDC from rescue). Sequence: `POST /agent/pricing` tier publish tx `34bYnZtJ…` → `/escrow/v2/open` (nonce 1, 0.11 USDC, escrow `85G3oP…1Qv`) tx `EVNLUNkq…` — coverage preflight passed at the exact 0.1-SOL floor → depositor approve → `/escrow/v2/settle` tx `5HzGZW1p…` → status `pending`, fee EXACTLY 0.5% (500), principal 100000 reserved, pending PDA `476Ht3LN…` idx 0 → **decode-parity PROVEN** (on-chain pending == ledger byte-for-byte) → NEGATIVE PROBES: replay settle returned cached `pending` with NO re-send ✓; early finalize refused AT SIMULATION (0x17d4, pre-broadcast) and row stayed retryable ✓ → dispute window 2160 slots (~14 min) → `/escrow/v2/finalize` (cranked BY THE DEPOSITOR — permissionless-crank proof) tx `3vWvNGP8…` → terminal `settled`. **Conservation EXACT on-chain: vault 110000→109500(−fee)→9500(−principal); worker ATA +100000; treasury fee moved at settle only.** Rule E4: this is the evidence package — promotion to master + any flag change remains the founder's call.

### 2026-06-18 — support session — lean in-product support tickets (staging push; NEW table + route + UI, migration `0004`)
- **What:** `POST /api/support/tickets` (`apps/api/src/routes/support.ts`, mounted `/api/support`). Fileable by user / connected-agent / guest (identity: Lucia → `resolveAgentSession` → guest fp). Zod-validated (category enum + ≤4000-char message + light context), per-subject in-memory rate-limit (5 / 10 min). Persists to NEW `support_tickets` table (append-only, **no FK**, migration `0004_support_tickets.sql`, idempotent additive — CREATE TABLE IF NOT EXISTS + 3 indexes), then best-effort PLAIN-TEXT Telegram relay via `ITACHI_DEBUG_*` (fail-open; no parse_mode so user content can't inject).
- **UI:** `SupportLauncher.tsx` (button + modal, light-on-dark, Esc/backdrop close, mobile maxHeight scroll) + `lib/support-client.ts`. Mounted on the cove interior (floating top-right, clear of joysticks), `/cove/history` nav, and `/cove/verify/event/[id]` nav (pre-tags the eventId → "dispute this hand"). Lean: no admin triage UI (status column exists for later).
- **Migration note:** applies via the CI migrate gate (`migrate-ci.ts`, forward-only/idempotent) on the staging push — NOT `db:push --force`. Adds ONLY the new table; touches nothing existing.
- **Verified:** api tsc clean on support.ts + index.ts; `bunx next build` exit 0. Codex pass on the route + live staging smoke (file a ticket end-to-end): pending in this session.
- **For:** the founder — players/agents can file a support ticket from the Cove; it lands in your itachi-debug Telegram. Branch `feat/support-tickets` → PR to `staging`. Not on master.

### 2026-06-18 — cove fairness session — client Verify UI un-stub for blackjack/hold'em/baccarat (staging push; frontend-only)
- **Bug:** `/cove/verify/event/[id]` dispatched blackjack/hold'em/baccarat to a `FutureVerifierPlaceholder` reading *"the verifier ships in Phase 6.7.1… reveal pending engine migration."* — STALE. The server-authoritative commit-reveal engine + `/verify` endpoint are LIVE (and just fixed, prior entry), so the placeholder actively MISLED players into thinking their hands aren't verifiable. Found in the same browser pass as the /verify fix.
- **Fix:** new `ServerEventVerifier.tsx` (two layers): (1) TRUSTLESS in-browser commitment check `sha256(revealedServerSeed)===serverSeedHash` via WebCrypto (`sha256Hex`) — the player confirms the seed was committed before the hand, trusting nobody; (2) server replay attestation via `fetchEventVerdict()` (the now-correct endpoint). Renders verdict + commitment chain + stored outcome (and expected on divergence). Slots keeps its full client-side replay. Dead `FutureVerifierPlaceholder` + `FUTURE_PHASE` removed.
- **Verified:** `apps/web` tsc — my 2 files clean; `bunx next build` exit 0. Live browser check on staging after deploy: pending in this session.
- **For:** the founder — players can now actually verify blackjack/hold'em/baccarat hands (with a trustless local hash check). Branch `fix/cove-verify-client-unstub` → PR to `staging`. Not on master.

### 2026-06-18 — cove fairness session — provably-fair /verify false-negative FIX (staging push; backend-only, no schema)
- **Bug (found via LIVE staging /verify):** `GET /api/cove/history/:eventId/verify` returned `verified:false` while `hashMatches:true` and every outcome value was byte-identical — for EVERY honest blackjack/hold'em/baccarat hand. Root cause: the comparators (`cove-verify-compat.ts` `jsonEq`, and the slots `winningLines` compare in `cove-history.ts`) used a raw **order-SENSITIVE** `JSON.stringify`. `expected` is freshly serialized (insertion-ordered) while `stored` is read from a Postgres **jsonb** column, which reorders object keys (length, then bytewise) → the strings never match. **Player-facing impact: the "verify fairness" feature told every honest player the casino cheated.** The unit tests missed it because they build the stored side with JS literals / `JSON.parse(JSON.stringify())`, both insertion-order-preserving — never reproducing jsonb's reorder.
- **Fix:** `jsonEq` → recursive `canonicalize` (sort object keys; **preserve array order** so card/reel sequences stay positional), exported as `canonicalJsonEq` and reused in the slots branch (`winningLines` is an array of objects jsonb also reorders). Fixes blackjack + hold'em + baccarat + slots at once. No schema, no new endpoint, no wire/contract change — purely the equality op.
- **Verified:** new key-shuffle regression tests (`reorderKeys` sim of jsonb) — `bun test cove-verify-compat.test.ts` = **26 pass / 0 fail** (4 new cases; each asserts the reordered row really differs in bytes AND still verifies TRUE, and a tampered value still fails). Full `apps/api` `tsc` — my 2 files clean (30 pre-existing baseline errors unrelated). Live re-verify on staging after deploy: pending in this session.
- **For:** the founder — provably-fair verification now reports TRUE for honest hands. Branch `fix/cove-verify-canonical-eq` → PR to `staging`. Not on master yet.

### 2026-06-17 — perf4 / S2 jump — v3 squat foot-grounding (prior grounding was a no-op) (staging push; `04591040`)
- **Why a v3:** a headless integration test (parse the real Milady VRM + `_emotes.glb`, run the REAL `retargetMixamoClip`) proved the `squat` clip is ROTATION-ONLY — raw `mixamorigHips.position` Y is a flat constant (104.226, 2 keyframes, ZERO descent). So the v1 `getSquatGroundLift` (sample a hip-Y descent) returned 0 every frame → **complete no-op**; bug #1 (midair squat) was NOT fixed by what shipped at `0effc747`/`ed865ae6`. A second attempt (procedural `crouchDepth` + foot-replant) also fails — lowering a rigid body then replanting the feet CANCELS to ~0 (you can't synthesize a crouch by translating a rigid body). **Honest note:** the v1 "verified 0.89→0.63 × 168.75 = 44wu" numbers (relayed from the 3da sub-agent) were never real — the asset has no descent; trusting unverified agent numbers is what let the no-op ship to staging.
- **v3 fix (FOOT-GROUNDING):** after `animator.update()` poses the rotation-only crouch, `group.position.y -= (getFootWorldYMin() − effectiveFloorY)` plants the lowest foot on the floor; since the knee-bend lifts the feet, this lowers the body so it settles toward the planted feet = squat DOWN (the direction the user asked for). `player-avatar.tsx` + `arena-npcs.tsx` mirror. Dead code removed (getSquatGroundLift, crouch constants, etc.).
- **Verified:** `bunx next build` exit 0. Headless harness measured the clip's knee-bend foot-rise = ~2.3 wu (Milady) → that's the achievable crouch depth (shallow). **NOT visually verified** — the live-browser drive could not trigger the possessed-avatar squat headlessly; depth/feel is a founder cold-test. Bug #2 (chargeMode halt-slide / run-skip) unchanged + still works.
- **For:** the founder — cold-test the squat on staging (does it settle DOWN with feet planted, no midair tuck?). If the ~2.3wu crouch is too shallow to read as a squat, the follow-up is a re-baked `squat` clip with knee-bend + root descent (blend007 + `_emotes.glb` rebundle + `?v=` bump) — NOT another runtime body-translate (proven impossible). Then promote S2 + S1 `staging → master` (both frontend-only, no migration).

### 2026-06-17 — perf4 / S2 jump-anim session — squat-down + halt-slide + run-skip (staging push; `0effc747`)
- **Changed:** Fixed staging-issue **S2** (jump animation) — frontend-only, no schema. BUG1 "midair squat": `'squat'` was in `IN_PLACE_CLIPS` (vrm-character-animator.ts) → its hip-Y descent track stripped → rotation-only → bent-knee rotations pulled the FEET UP to a pinned pelvis. Fix: removed `'squat'` from the strip set (descent survives retarget) + lift the avatar group by the descent each frame (`getSquatGroundLift()` samples hip-Y keyframes, no bone read/alloc) so pelvis lowers + feet stay planted. BUG2 "foot-slide": added `jumpState.chargeMode` (`'squat'|'run'|'none'`), set on the charging rising edge from speed class — walk/idle → halt WASD + squat wind-up; run → keep running, skip squat, jump on release. Mirrored on the possessed-player NPC path (arena-npcs.tsx); mobile parity automatic. Also carries the **3da game-skill-suite training** commit (`.claude/` tooling, deploys nothing).
- **Magnitude bug caught in orchestrator review (audit team had APPROVED, missed it):** the retargeter (`mixamo-retarget.ts:299`) keeps ABSOLUTE scaled hip-Y (not zeroed-at-rest), and the lift is added to the WORLD-unit outer group while the hip track is VRM-normalized meters. First cut returned `-hipY` (negative → the `if(lift>0)` guard made it a complete NO-OP → feet would sink ~44wu — the original 2026-05-22 sink, via a new path). Corrected to `(restHipY − currentHipY) × fitScale` where fitScale = the avatar's own `vrmRenderScale` (the exact scale on its `<primitive>`). 3da instrumented real numbers: Milady rest 0.89 / deepest 0.63 VRM-m × 168.75 wu/m = ~44wu lift = the visible descent; 0 at standing.
- **Verified before+after push:** `bunx next build` exit 0 (pre-rebase, post-magnitude-fix, AND on the rebased tree combining the concurrent blackjack/land changes); grounding math numerically verified; CI migrate(no-op)→deploy green; **served-bundle grep confirms `getSquatGroundLift` live** in chunk `0-48_iij7_it4.js` on `staging.clawville.world` (Coolify flipped ~5 min after push) and `cv_last_load_ms` (S1) still present. NOT visually verified — software-render localhost can't show jump feel; needs founder cold-test (Rule E4).
- **3da-led team `s2-jump-anim-2026-06-17`** (impl + spec/regress/adversary sub-team; orchestrator caught + reconciled the magnitude bug — lesson recorded: 3da audit teams can miss world-unit-vs-VRM-normalized SCALE bugs in grounding math). Same-diff doc: `3dStructure.md §6z §7a`.
- **For:** the promotion session — S2 + S1 are frontend-only (no migration); promote `staging → master` once the founder signs off the cold-tests. S2(orig) backlog item is the next-after-this until that sign-off.

### 2026-06-16 — blackjack-soft-lock session — resume HARDENING: audit follow-up (staging push; server+client, no schema)
- **Changed:** Follow-up to the #122 blackjack resume fix, resolving all 5 issues a 4-lens adversarial audit found. 3 files (`apps/api/src/routes/cove-blackjack.ts` +145, `apps/web/.../BlackjackModal.tsx` +92, `apps/web/.../blackjack-api-client.ts` +26):
  - **#1 (client, a regression #122 introduced):** the eager-restore awaited `/hand/current` while DEAL was tappable; a stale `{hand:null}` could `setHand(null)` and wipe a just-dealt hand → soft-lock recurs. FIX: `restoreHandFromServer(handRes, allowClear=true)`; eager-restore-on-open passes `allowClear=false` (never clears on open); autonomous resync keeps `true`.
  - **#2 (server, pre-existing):** guests permanently soft-lockable mid-hand (every recovery endpoint 403s guests; `/session/open` resumes the orphan; no sweeper). FIX: GUEST-ONLY orphan auto-recovery in `POST /session/open` — under the existing shoe `FOR UPDATE` lock, void the orphan hand (`WHERE status='in_progress'` idempotency guard; demo stake forfeit) + close the orphan shoe + fall through to a FRESH shoe (drift-free). `!isLedgerSubject` gate ⇒ authed + AGENT shoes are NEVER auto-aborted (their client restores+finishes).
  - **#3 (server+client, defeated the PR headline "resume a split hand"):** mid-split restore focused a RESOLVED-but-not-bust sub-hand (stood-21/doubled/surrendered/split-ace) → actions 400 `sub_hand_already_terminal` → re-lock. FIX: new server-authoritative `isResolved` per-sub-hand flag (extracted `subHandResolved` SSOT, also now backs `isHandTerminal`) on BOTH `/hand/current` + `/action` in-progress; client adds `isResolved` to the types + `SubHandView`, focuses `findIndex(!isResolved)` in `restoreHandFromServer` AND `mergeActionInProgress` (also fixes a pre-existing live-split mis-focus), and GATES Hit/Stand/Double/Surrender on the active sub-hand `!isResolved`.
  - **nits:** `/action` `dealerUpcard ?? null` one-shape parity; a deterministic 500 `hand_peek_failed` now surfaces a "refresh to retry" toast instead of the misleading "Resumed your open shoe."
- **Money-safety:** NO settlement logic touched; `isResolved` is derived from the already-computed peek/script (no new engine run, no hole-card/seed/shoe reveal); guest recovery is demo-CT only, race-safe under the shoe lock, never touches authed/agent hands. No new endpoint; no `PROTOCOL_VERSION` change (additive field agents may ignore).
- **Verified before push:** web `bunx next build` exit 0; api+web `tsc` clean on the 3 edited files (remaining tsc output = pre-existing unbuilt-workspace + dual-`@types/three` noise); blackjack tests **39 pass / 0 fail** (engine + route); full team (2 parallel implementers on a frozen `isResolved` wire-contract + adversarial re-audit = **SHIP**, no remaining items). I reviewed the server diff myself.
- **NOT done (Rule E4/E5):** NOT browser-verified — the split-resume, guest-orphan-recovery, and deal-during-restore paths need a live staging exercise + founder sign-off before "done"/promotion. Live-smoke planned on staging post-deploy (casino-engine live-smoke lesson).
- **For:** the promotion session — promote with the #122 fix together (no migration). Same prod soft-lock (authed + guest + split) persists on `master` until then.

### 2026-06-16 — blackjack-soft-lock session — Cove blackjack resume-on-open soft-lock fix (staging push; frontend-only)
- **Changed:** `apps/web/src/components/cove/blackjack/BlackjackModal.tsx` only. ROOT CAUSE of a reported PROD soft-lock (user stuck with a perpetually open bet — empty card areas + DEAL button, DEAL toasts "Finish the current hand before dealing another"): the "Eager restore on open" effect restored the open SHOE (`/session/current`) but NOT an in-progress HAND. The server keeps one `blackjack_hands` row `in_progress` per shoe and blocks a new `/hand/deal` (`hand_in_progress`) AND blocks `/session/close` (`shoe_has_in_progress_hand`) while it's live — so a player who dealt then closed/refreshed/reopened the modal mid-hand was permanently soft-locked on the betting UI with no way to finish OR deal. The hand-restore logic already existed (read-only `GET /hand/current` + the client rebuild) but was wired ONLY into the autonomous-agent `resyncAfterStaleDecision` path. FIX: extracted the rebuild into a shared `restoreHandFromServer` `useCallback` and called it from the eager-restore-on-open path too (after the shoe restore), so reopening mid-hand shows the cards + Hit/Stand → player finishes the hand → settles → shoe freed. +64/−39, one file.
- **Money-safety:** read-only — `/hand/current` does NO mutation/ledger; the bet was already debited at deal; finishing settles normally. Self-adversarial review covered: null/none response (clears, betting UI stays), `cancelled` guard before every setState, split-focus, insurance-offered carry-through, no double-charge, phase derivation (Hit/Stand render from hand!=null && settled==null — same shape the autonomous resync produces, which is proven).
- **Verified before push:** `tsc` clean on the file; `bunx next build` exit 0.
- **Affected prod user:** being unstuck separately via a surgical prod-DB resolution of their one stuck `in_progress` hand (founder-approved; shown + confirmed before mutating). The deploy fixes it durably for everyone-on-reload.
- **For:** the promotion session — promote `staging → master` (no migration, pure frontend) after staging sign-off. Same prod soft-lock exists on master until then.

### 2026-06-16 — poker-0003-migration session — poker/special-events CI migration + FK-drift convergence (staging push; schema `0003`)
- **Changed:** Authored `packages/database/migrations/0003_poker_special_events.sql` — the CI-gate migration for the 8 poker MTT + special-events tables (`poker_blind_schedules, special_events, poker_tournaments, poker_tables, poker_tournament_entrants, poker_hands, poker_tournament_results, special_event_signups`). The poker/event CODE already merged to staging (PRs #113/#115) but had **no committed migration**, so a `staging → master` promotion would ship poker code to prod with no poker tables → prod 500s. `0003` closes that gap. It is idempotent + additive + **FK-convergence-aware**.
- **Why convergence:** the 8 tables were originally recovered on the staging DB (after a `db:push --force` dropped them) via UNNAMED inline `REFERENCES`, so Postgres auto-named their 13 FKs `<table>_<col>_fkey`. drizzle expects `<table>_<col>_<reftable>_<refcol>_fk`; an `*_fkey` makes `drizzle-kit push` churn DROP/ADD forever. `0003` emits, per FK, `DROP CONSTRAINT IF EXISTS <drifted *_fkey>` + a `DO $$`-guarded `ADD CONSTRAINT <drizzle *_fk>` — so empty PROD gets the drizzle name from the start and drifted STAGING converges to it; everything else is `CREATE [TABLE|INDEX] IF NOT EXISTS`. Only drift was the 13 FK names; every column/CHECK/index was already drizzle-correct on staging.
- **Verified before push (Rule E5/E4 — both empirical, against the LIVE staging DB, all in a rolled-back scratch schema so nothing persisted):** (1) FROM-EMPTY path → applied `0003` into a fresh `_mig_probe_0003` schema, introspected, diffed vs the live `public` poker/event schema = **ZERO DIFF** for all 8 tables (columns/types/nullability/defaults/CHECK bodies/indexes byte-for-byte; FK defs identical with the drizzle `*_fk` names). (2) CONVERGENCE path → applied `0003`, RENAMEd the 13 FKs to the exact drifted `*_fkey`, re-applied `0003` = **all 13 converged back to `*_fk`, zero `*_fkey` remain**. Tool: `packages/database/scripts/_mig-tool-poker.ts` (staging-only, hard prod-guard, never prints creds — throwaway, not committed). Authored by a full collaborative team (`poker-0003-migration`: 2 independent implementers + reconciler + adversary; adversary APPROVED all 7 checks: idempotent re-run, convergence, from-empty order, column/constraint/index parity, txn-safety, no-DROP-of-data-tables).
- **Watch on push:** CI `migrate` job applies `0003` to the staging DB BEFORE the Coolify deploy (converges the FK names; no-ops the tables). If `migrate` fails it BLOCKS the deploy — but `0003` is pre-validated end-to-end. Re-introspect staging after to confirm the 13 FKs are now `*_fk`.
- **For:** the poker PROMOTION session — `0003` is the LAST gate-blocker for poker prod. On `staging → master` the prod `migrate` job CREATEs the 8 tables on the empty prod DB. Still gated on: founder Rule E4 sign-off of a live played hand on staging + the 10-agent stress harness run + the PROTECTED partner-surface agent-tools pass (Codex + mock-Hatcher). `0003` once applied is checksum-frozen — never edit; a fix needs `0004`.

### 2026-06-16 — S1 loading-screen session — bar bands rebalanced + slow-hint self-calibrated (staging push)
- **Changed:** Fixed staging-issue **S1** (loading screen inaccurate) in `apps/web/src/components/game/sea-loading-screen.tsx` ONLY — pure frontend, no schema/API. Two complaints: (1) "Taking longer than expected" fired on EVERY cold load — perf round-3 "Change D" had hard-coded `SLOW_MS=8_000`, *below* the real ~10s warm / ~22s cold load, so it was noise. Replaced with a self-calibrating threshold: each successful mount→`__W3D_READY` is recorded to `localStorage['cv_last_load_ms']`, hint fires only at `max(12s, lastLoad × 1.8)` (first-load default 22s); `TIMEOUT_MS` 30s→45s. (2) Bar jumped to ~60% instantly — `__W3D_PROGRESS` (`loaded/total`) only tracks GLBs (VRMs use raw fetch, invisible), so the GLB batch draining spiked it to 1.0 and the old 0.60 download band credited 60% via the ratchet. Bands rebalanced download `[0,0.30]` · upload `[0.30,0.85]` (dominant, incrementally-tracked) · compile `[0.85,0.97]`; download band time-eased (`min(downloadFrac, elapsed/4s)`) so the spike climbs from 0; new `preparing` ("Preparing scene…") phase for the GLB-done-but-upload-not-started gap.
- **Verified before push:** `@clawville/web build` exit 0; `tsc -p apps/web` shows 0 errors in the edited file. Deterministic in-browser test (local prod bundle :3210, chrome-devtools, bridge values pinned behind getters to isolate from the real canvas): bar starts at 0 on a `prog=1` spike (NOT 60), download/preparing ceilings at 30%, upload climbs 30→58→85 with the counter, texReady/canvasReady→97, `ready`→overlay dismissed; phase labels correct; `cv_last_load_ms` recorded from a real prior load (26843 on the software renderer). Screenshot of the upload-phase overlay (bar 56% @ upT=179/upD=84, "Uploading to GPU…") captured in-session.
- **NOT yet validated (needs founder, Rule E4):** the actual *feel* on real Iris Xe + whether "Taking longer" stops firing on a genuine cold reload — software-render localhost inflates load timing and can't reproduce it. Do a cold reload (hard-refresh / clear cache) on `staging.clawville.world/game`.
- **Same-diff docs:** `docs/perf-round3/baseline-2026-06-15.md` "Loading bar" section updated to the new bands + slow-hint scheme.
- **For:** next session — on sign-off, promote `staging → master` (no migration, pure frontend). Then S2 (jump anim) is next in the backlog.

### 2026-06-16 — S3 spawn-recenter session — PROD PROMOTION (PR #116) + verified live
- **Changed:** Promoted the S3 spawn fix `staging → master` (PR #116, merge `802c5f2a`). Prod CI ran: `migrate` job applied `0002_avatar_spawn_recenter.sql` to the **prod** DB (BLOCKING step, 37s, vs `PROD_DATABASE_URL` GH secret) → `deploy` queued Coolify. This fixed prod's *existing* S3 bug: prod already ran the 18432 client (from PR #114) but its `avatars` table still defaulted to the old `2560` center, so logged-in players on stale rows seated off-center.
- **Verified live on prod (Rule E4 — founder signed off the staging playtest first):** polled both prod containers until both flipped from `95b9f1e7` → `802c5f2a` (Coolify async ~4 min after the CI `deploy` queue); `curl https://api.clawville.world/health` = ok, web `/game` = 200; chrome-devtools fresh isolated guest on `clawville.world/game` → explore-mode camera centered on X=9216 (world `0,600,1300`), world renders correct (Town Center dead-center, NPC ring, no clipping/atmosphere occlusion); switched to **NPC Mode** → "You" body spawned in front of Town Center, SONAR read **`9216,9756`** (the `SPAWN_PX`, not the old `5760,6300` diagonal). Guest avatar `Guest6961` Lv1 created at spawn. (Verification screenshots captured in-session, not committed.)
- **Prod SSH note:** the prod box key (`~/.ssh/clawville_hillsboro`) is passphrased and git-bash's ssh-agent wasn't connected — use **PowerShell's native `ssh`** (talks to the Windows ssh-agent service, which had the key loaded) for prod container `SOURCE_COMMIT` reads. Prod Coolify container UUIDs: api=`ebnatuxblgp4q0antoca9swk`, web=`ds7hoho685ire522lz3hie2j`.
- **For:** next session — S3 is DONE on prod. No pending migrations. Resume the staging backlog (S1 loading bar next) on a fresh branch off latest `origin/staging` in worktree `cv-staging-s3`.

### 2026-06-16 — S3 spawn-recenter session — avatar spawn re-center single-source-of-truth (staging push; schema `0002`)
- **Changed:** Fixed staging-issue **S3** (player/NPC spawn at a corner-ward diagonal). Land Phase 0 grew the CLIENT world 5120→18432 (center 2560→9216) but never migrated the SERVER/DB: `avatars.position_x/y` defaulted `2560`, the live Hono validators (`avatars.ts` `updatePositionSchema` PATCH `/me` + `heartbeatSchema` `/me/heartbeat`) capped at `max(5120)` (rejecting the correct `9216` spawn), `world.ts` `TOWN_CENTER` was `2560`, and `npc.ts` `spawnPlayerNpc` hardcoded the **11520-era** center `(5760,6300)` (the user-visible NPC-mode diagonal). FIX: one shared SSOT `packages/shared/src/constants/world-dimensions.ts` (`WORLD_PX_WIDTH/HEIGHT=18432`, `WORLD_CENTER_PX={9216,9216}`, `SPAWN_PX={9216,9756}`) read by client (game.ts dev-assert + npc.ts spawn), API (world.ts TOWN_CENTER + PATCH/heartbeat bounds + `resolveAvatarMeta` out-of-bounds clamp), DB defaults (`9216,9756`). Migration **`0002_avatar_spawn_recenter.sql`** resets all avatars + column defaults.
- **Drift audit (S3 part 2):** the prior session's "lost direct-to-master changes" = a PayAI press release (6 `(direct to master)` commits) — already reconciled onto staging (press-release.tsx/page.tsx/press images byte-identical staging==master). No actionable prod-only code drift remains.
- **Verified before push:** `0002` dry-run = recognized as the single pending migration; execute-and-rollback against the live staging DB = all **4 staging avatars were at `2560,2560`** (bug confirmed), reset cleanly to `9216,9756`, rolled back (no premature mutation). Gates: `bunx tsc --noEmit` shared/database/api exit 0; web `bunx next build` ✓ compiled (per-package web `tsc` = the known 372-error dual-`@types/three` baseline, 0 new). (`avatars.test.ts` is an env-gated integration test — fails at signup without `DATABASE_URL`, unrelated.)
- **Watch on push:** CI `migrate` job applies `0002` to the staging DB (resets staging avatars to spawn) BEFORE the Coolify deploy. If `migrate` fails it blocks the deploy — but `0002` is pre-validated. Workflow team `staging-s3-spawn-2026-06-16` (impl + 3 auditors; regression auditor caught that the implementer had fixed only the DEAD BFF route, not the live Hono validators — fixed). Adversary caught that the original "land Phase 0 unpromoted" premise was FALSE (it's on prod via PR #114) — docs corrected.
- **For:** the promotion session — `0002` must reach prod to fix prod's existing S3 bug; it ships at the next `staging → master` PR (the CI prod `migrate` job applies it, realigning prod rows to the 18432 client already live there). Needs founder sign-off on the staging playtest first (Rule E4).

### 2026-06-16 — migration-gate session — PROD PROMOTION (PR #114) + agent-stray-write incident + fix
- **Changed:** Promoted `staging → master` (PR #114, merge `95b9f1e7`) — shipped the migration gate + land Phase 0 (world re-grow 360→576 + schema) + perf-round3 + cove agent-parity + hatcher docs to prod. Prod `migrate` job ran, prod deploy succeeded.
- **Broke / root cause (caught in post-deploy verification):** prod's land tables came up with the WRONG schema — unnamed `*_fkey`/`*_key` constraints + 2 stray CHECK constraints (the drift the gate exists to prevent). Root cause: during the build workflow, a sub-agent ran a migration test against the **prod** DB because `packages/database/.env.local` carried the **prod** `DATABASE_URL` (+ `ELIZA_ALLOW_DESTRUCTIVE_MIGRATIONS=true`), which Bun auto-loads for scripts run from that dir. It applied the *pre-fix* (unnamed-constraint) `0001` to prod, so the real gate's `CREATE TABLE IF NOT EXISTS` then skipped. (This is exactly the "prod URL in an env file is a disaster vector" hazard.)
- **Fix:** (1) repointed `packages/database/.env.local` `DATABASE_URL` → staging. (2) Dropped the 8 EMPTY prod land tables + cleared the `0001` tracking row, re-ran `migrate-ci.ts` against prod with the FINAL `0001` → recreated with correct drizzle-named constraints. Verified: 20 named FK/unique, 0 unnamed, 0 stray checks, 8 tables. Prod now == staging == drizzle.
- **Security:** the prod DB password is now in agent transcripts — rotation is the open follow-up (see CURRENT STATE).
- **For:** every session — NEVER put a prod URL in any `.env*`. Bun auto-loads `<cwd>/.env.local`; a script that targets `DATABASE_URL` from `packages/database/` would hit whatever's there. Keep all local env files staging-only.


- **Changed:** Added a CI migration gate — `packages/database/scripts/migrate-ci.ts` (forward-only, idempotent, `_clawville_migrations` tracking table; NOT drizzle-kit push/migrate) + `packages/database/migrations/0001_land_economy.sql` (8 land tables + 9 enums) + a `migrate` job gating `deploy` (`needs: migrate`) in BOTH workflows. Also: `wager.ts` `default(0n)`→`default(sql\`0\`)` (unblocks `db:push`/`generate` repo-wide — drizzle-kit 0.24 can't serialize a BigInt literal), `paths-ignore` docs-skip on both workflows, this `deploy-status.md` + rule, and staging-DB-isolation doc updates.
- **Why it exists:** staging + prod are now SEPARATE Supabase DBs (split 2026-06-16) and Coolify never runs migrations — so a schema change on staging would NOT reach prod on merge, and prod would crash querying a missing table. The gate applies pending migrations and BLOCKS the deploy if they fail.
- **Verified:** `0001` reproduces drizzle's staging schema EXACTLY (isolated scratch-schema diff on the live staging DB — all 8 tables / 9 enums / columns / named FK+unique constraints / indexes match). Runner proven end-to-end against the real Supabase session pooler (dry-run → apply → idempotent re-skip) + ephemeral-Docker (8 scenarios incl. per-file atomic rollback + immutability guard).
- **Gotchas found (for the next author):**
  - drizzle names EVERY constraint — `<table>_<col>_unique`, `<table>_<col>_<reftable>_id_fk`. Unnamed inline `UNIQUE`/`REFERENCES` auto-name `*_key`/`*_fkey` → `drizzle-kit` churns DROP/ADD forever. The empirical scratch-diff caught 17 FK + 3 unique mis-namings the adversarial review missed.
  - drizzle-kit 0.24.x does NOT emit CHECK constraints — staging lacks `land_structure_level_range` + `service_listings_price_non_negative` that `land.ts` declares. `0001` omits them too (pure parity; both bounds are app-enforced). A future `0002` can add them to BOTH dbs if we want DB-level guards.
  - Migrations are IMMUTABLE once applied (checksum-locked). To change one, add a NEW file. `ALTER TYPE … ADD VALUE` must live ALONE in its own file (can't run inside the per-file implicit txn).
- **For:** the next deploy session — the gate now guards every staging + prod deploy. If a deploy is "stuck pending", first check the `migrate` job in the GitHub Actions run, not just Coolify.

### 2026-06-16 — land Phase 0 + staging-DB isolation — world re-grow + DB split (incident)
- **Changed:** Land economy Phase 0 (world re-centered 360→576 tiles; `land` schema). Split staging onto its OWN Supabase project (`mtpixvtclsjqjguouxes`); repointed Coolify staging api/web; prod DB untouched.
- **Broke / root cause:** the Phase 0 schema-index commit captured an uncommitted `export * from './poker'` (poker.ts not committed) → `TS2307` broke EVERY staging build (mine + other sessions') from ~11:57 until fixed at `aff9d6c1`. Separately, `wager.ts` `.default(0n)` crashed `db:push`/`generate` repo-wide (BigInt serialize).
- **Fix:** removed the leaked `./poker` export (`aff9d6c1`); `wager.ts` BigInt fix folded into the 2026-06-16 migration-gate bundle above.
- **For:** the poker session — your `poker.ts` / `central_messages` tables exist on the staging DB as drift but are NOT in the committed staging schema, so they will NOT be pushed to prod until you commit `poker.ts` + add a `0002`-style migration. The migration gate is additive-only and will never drop them.
