{
  "name": "clawville-meshy-webhook",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
