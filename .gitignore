# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
.next
out
build

# Environment files
.env
.env.*
!.env.example
!.env.deploy.example

# IDE
.idea
.vscode
*.swp
*.swo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage
.nyc_output

# Turbo
.turbo

# Claude Code — per-user / runtime state (never commit these)
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/projects/
.claude/worktree-archive-*/
.claude/settings.local.json
.claude/reports/

# Misc
.DS_Store
*.pem
*.log
nul
cookies.txt

# TypeScript
*.tsbuildinfo

# Drizzle
drizzle/*.sql

# ElizaOS local database
.eliza/

.vercel
.env*.local

# Railway env backup (pre-decommission snapshot — contains secrets)
scripts/deploy/railway-env-backup.json
scripts/deploy/treasury-*.json

# Local scratch / prompt notes (never committed)
miladyApp.txt
*.scratch.md

# Working drafts — local planning docs for the active rebuild
3dStructure.md
GameFeatures.md
AgentHosting.md
improvements.md

# Draco compression originals — pre-compression GLB backups. Git history
# already holds the original uncompressed versions, so revert is a
# `git checkout <sha> -- <file>.glb`. These are ~17MB of local-only restore
# material and shouldn't bloat the repo.
apps/web/public/models/.draco-backup/
# WebP texture compression originals — same rationale as .draco-backup above.
apps/web/public/models/.webp-backup/
# KTX2 compression originals — pre-compression WebP backups.
apps/web/public/models/.ktx2-backup/
# C6 meshopt+WebP pipeline originals — pre-C6 GLB/VRM backups (regeneratable).
# Revert a single file: git checkout 13480db -- apps/web/public/models/guide.glb
apps/web/public/.assets-backup/
.worktrees/
